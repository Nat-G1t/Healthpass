# HealthPass — Product Requirements Document (PRD)

**Web-Based Scheduling and Digital Medical Clearance System with Self-Service Vitals Kiosk**
Pampanga State University · College of Computing Studies · Capstone Project

---

## Document Control

| Field | Value |
|---|---|
| Document title | HealthPass Product Requirements Document |
| Version | 1.0 (Baseline for build) |
| Date | June 11, 2026 |
| Status | Approved for development — Week 1 of 12 |
| Prepared by | Nathaniel C. Medina (Lead Developer — Web Application) |
| Hardware lead | Baldo (Kiosk hardware & Web Serial integration) |
| Documentation / QA / Coordination | David, Dela Cruz, Fabian, Pamintuan, Sebastian |
| Faculty adviser | Andrei Viscayno |
| Target completion | August 30, 2026 (full working prototype; internal freeze August 28, 2026) |
| Source artifacts | `HealthPass_Context.md` (system spec), `HealthPass_Project_Plan.md` (build plan), Claude Design HTML prototypes (web app + kiosk), finalized 10-table ERD |

### Revision History

| Version | Date | Author | Changes |
|---|---|---|---|
| 0.x | Pre-June 2026 | Team | Earlier AI-powered kiosk concept (superseded — see §1.3) |
| 1.0 | June 11, 2026 | N. Medina | Baseline PRD for the no-AI HealthPass system. Incorporates locked schema decisions: BP flag threshold 140/90, `vital_signs.entry_method`, `batch_requests.scheduled_date`, clinic capacity as config value. |
| 1.1 | June 16, 2026 | N. Medina | Added optional middle_name to student_profiles and the registration Step 2 form, aligning capture with the Middle Name field on official form DHVSU-QSP-OSS-004-FO002-R03. |
| 1.2 | June 30, 2026 | N. Medina | Three spec changes: (1) login page becomes a two-panel layout (login card + right illustration panel, illustration shown whole/centered, collapses on narrow screens; card internals unchanged) — FR-AUTH-08; (2) student college becomes self-editable on transfer, with analytics frozen to a capture-time snapshot via a new `clinic_visits.college_id` column — FR-STU-09 (schema add flagged); (3) new kiosk walk-in confirmation gate after Identity Confirm when no medical appointment is booked today — FR-KSK-03a / Decision D-16 (submit-time appointment linkage unchanged). |
| 1.3 | June 30, 2026 | N. Medina | Completed the college-transfer analytics policy: re-pointed the Flagged Anomalies College column (FR-ANL-05) and Cases-by-Medical-System source (FR-ANL-08) to the capture-time `clinic_visits.college_id` snapshot, mirrored in HealthPass_Context.md, and formalized the policy as Decision D-17 (editable college → live admin scope + frozen historical analytics). No schema change beyond the v1.2 `clinic_visits.college_id` add. |
| 1.4 | July 9, 2026 | N. Medina | Print view (Module PRT) corrections from the official-form review, plus two encode features. (1) Student No. removed (DHVSU-QSP-OSS-004-FO002-R03 has no such field); Case Category no longer printed under REMARKS (physician's manual annotation); pregnancy/LMP pre-fills from the kiosk questionnaire. (2) "Physical Signs Disorder of" exam-findings fieldset on the Doctor's Assessment screen (physician examines, nurse records; nine nullable boolean `ps_*` columns added to `clearance_records` — flagged schema change); the printed form shades its bubbles from those findings, and kiosk questionnaire YES answers pre-check their matching rows. (3) Medical Case Categories become **multi-select** — new `clearance_case_categories` child table (**table #11**, flagged), analytics count per record × category, student My Records chips every category. FR-NRS-03, FR-PRT-02, FR-ANL-02/03 amended; Decisions D-22, D-23. |
| 1.5 | July 12, 2026 | N. Medina | Print + encode refinements from in-flow review. (1) **"Others, Specify" purpose** — the Purpose dropdown gains the official form's fifth line; picking Others requires a free-text specify-event (max 120 chars). `clearance_records.purpose` enum → varchar(50) and new `purpose_other` varchar(120) NULL — **flagged schema change**; the printed form shades the "Others, Specify:" bubble and prints the event on its line. (2) **College removed from the printed Course, Year & Section line** (the official form has no college field; the earlier rides-the-course-value divergence is dropped). (3) **Kiosk NO answers now pre-fill their physical-sign rows** alongside YES (rows without a kiosk counterpart still open unanswered). (4) Print template default paper is **Letter** (A4 still fits one page). FR-NRS-03, FR-PRT-02, BR-16 amended; Decisions D-24, D-25. |
| 1.6 | July 12, 2026 | N. Medina | **Kiosk hardware change: 15.6″ 1080×1920 portrait touchscreen** replaces the 7″ 800×480 landscape panel. All kiosk screens restacked for portrait with `--k-zoom` retuned (2 / vitals 2.25) and larger touch targets; behavior, state machine, and endpoints unchanged. §2 out-of-scope, §4.6, NFR-4, §8.4, §11.1 and the design-system viewport updated; Decision D-26. Pi display-rotation config (Wayland/labwc) noted for the hardware track in `docs/deployment-pi.md`. |
| 1.8 | July 14, 2026 | N. Medina | **Batch clinic date is admin-proposed, Director-confirmed** — the College Admin picks a required Requested Clinic Date on the New Batch Request form (they know the cohort's event; the Director doesn't); the Director's approve modal pre-fills with it and may adjust (e.g. requested date passed while pending, clinic at capacity). New `batch_requests.requested_date` DATE NULL — **flagged schema change** (NULL only on pre-1.8 batches). FR-ADM-02/04, FR-DIRA-01/02 amended; Decision D-29 (supersedes D-5's director-picked date in part — the final date is still stamped into `scheduled_date` at approval). |
| 1.7 | July 12, 2026 | N. Medina | **Student-chosen clearance purpose at self-booking** — the Book Appointment page gains a third card (Medical only), the same locked Purpose dropdown as nurse encode (four values + Others, Specify), extracted into a shared `<x-hp.purpose-fieldset>` component. New `appointments.purpose` varchar(50) NULL + `purpose_other` varchar(120) NULL — **flagged schema change**; booking Form Request validates server-side (required for medical, Others needs specify text, nulled for dental). Nurse encode now shows its Purpose input **only** for visits whose appointment carries no purpose (walk-in / batch / pre-feature); when the student chose one, encode hides the input and carries the choice onto the clearance record so the printed form auto-populates unchanged. FR-STU-02/04, FR-NRS-03, BR-16 amended; Decision D-28. |
| 1.9 | July 15, 2026 | N. Medina | **Analytics matrix placement** — the FR-ANL-03 Summary of Medical Cases matrix renders inside the FR-ANL-02 chart's "View as table" toggle (replacing the chart's college-rows fallback table) instead of a separate card: both views present the same encoded college × category dataset, so one card now carries one dataset in two formats. Uncategorized encoded records get a card-level 'N encoded without category' footnote. No schema or data-scope change; FR-ANL-02/03 amended; Decision D-30. |
| 1.10 | July 15, 2026 | N. Medina | **Cases-by-Medical-System bars split by sex** — each FR-ANL-08 bar becomes a Male + Female stack: Male in the system's full-strength FR-ANL-02 series color, Female in a code-derived 50%-white tint of it; per-system total drawn at the bar's end, shade convention stated in the card subtitle instead of a legend. Source adds a `student_profiles.sex` join; counting rules and totals unchanged. No schema change; FR-ANL-08 amended; Decision D-31. |
| 1.12 | July 21, 2026 | N. Medina | **The internet deployment becomes the real deployment; the defense demos it.** (1) **Decision D-34** — HealthPass ships as one hosted Laravel app over HTTPS; at the defense the Pi is a **terminal**, with Chromium on `https://<domain>/kiosk`. **Supersedes D-9**, which is retained as the rehearsed offline fallback for venue-internet failure. Internet deployment moves from "optional, post-stability" to **in scope**. Deployment-level consequences only — no feature behaviour changes: `TRUSTED_PROXIES` required (never `*`; applied in `AppServiceProvider::boot()`, not `bootstrap/app.php`), `HEALTHPASS_KIOSK_ALLOW_LOOPBACK=false`, the Pi enrolled as a kiosk device (D-27) with `--incognito` **removed** from the launcher so the device cookie and Web Serial grant persist, and the ESP32 serial grant re-issued on the new origin (grants are per-origin). New runbook `docs/deployment-hosted.md`; `docs/deployment-pi.md` gains §10 (offline fallback). (2) **Decision D-35** — staff accounts stay **seeded** (FR-AUTH-05 unchanged, a Director-approved staff registration flow explicitly rejected) but ship with real emails and one-time passwords forced to change at first login, enforced by `RequirePasswordChange` middleware; schema change `users.must_change_password` (default false, students unaffected). §10 environments table and open question 4 updated; Decisions D-34, D-35. |
| 1.13 | July 27, 2026 | N. Medina | **Batch approval becomes confirm-only; rejection requires a written reason.** (1) **Decision D-36 — SUPERSEDES D-29's adjust-at-approval clause.** The Director's approve modal loses its date picker: a batch is approved on the `requested_date` the College Admin submitted, read from the locked row inside the transaction and never from the request body (`ApproveBatchRequest` now accepts **no** input, so a posted `scheduled_date` is inert). Two kinds of batch can no longer be approved at all, in the UI *and* at the endpoint: legacy batches with `requested_date` NULL (pre-D-29), and batches whose requested date **passed while they sat pending** — neither leaves a date to confirm, and the latter would schedule a cohort in the past. Both are directed to reject-and-resubmit; a batch requested for today is still confirmable. The escape hatch for a date the clinic can't take is **reject with a reason**, which leaves an audit trail a silent date move did not. FR-DIRA-02 amended; the FR-DIRA-06 capacity **warning** is unchanged. (2) Rejection now requires a reason (required, 10–500 chars) validated by a new `RejectBatchRequest` Form Request; new `batch_requests.rejection_reason` TEXT NULL — **flagged schema change** (NULL only on pre-1.13 rejections). FR-DIRA-04 amended. (3) The College Admin reads the reason back on Batch Tracking: a **Rejection Reason** column appears beside Status **only when the list contains a rejected row**, with a View button opening a modal showing the reason, the reviewing Director and `reviewed_at`; other rows show an em dash. FR-ADM-05 amended. D-29 marked superseded in place; Decision D-36. |
| 1.14 | July 27, 2026 | N. Medina | **Appointments get a time; capacity moves from per-day to per-hour.** **Decision D-37.** (1) The clinic day is now **ten bookable one-hour slots** (7–8 AM … 4–5 PM, **lunch included**), derived at runtime from `healthpass.clinic_hours` by the new `App\Services\ClinicScheduleService` — never hardcoded in a view. (2) Capacity becomes two config values: `hourly_capacity` **12** (a kiosk session takes at most 5 minutes) and `daily_capacity` **120, raised from 40**. **Medical and dental share one counter** — the constraint modelled is clinic congestion, not kiosk throughput. (3) Flagged schema changes: `appointments.scheduled_time` TIME NULL + index(`scheduled_date`,`scheduled_time`,`status`), and `batch_requests.requested_time` TIME NULL + `requested_blocks` TINYINT NULL (span start + LENGTH, never an end time). Pre-D-37 rows keep NULL and are **deliberately not backfilled**; they render as "—" and are seen only by the daily cap. (4) Students pick a slot after a date (FR-STU-03/04); College Admins pick a start hour and the system computes a `ceil(students ÷ 12)`-hour span (FR-ADM-04); the Director confirms that span read-only (FR-DIRA-02). (5) **FR-DIRA-06 changes from warn-but-allow to a HARD BLOCK** — with D-36's confirm-only approval, a batch whose hours filled while it sat pending can only be rejected and resubmitted, which is the intended workflow. (6) New BR-21 (per-hour cap), BR-22 (batch span) and **BR-23 (elapsed hours: on today an hour stops being bookable once it has ENDED — 11–12 dies at 12:00 — enforced for both the student picker and the College Admin's batch start hour, and refusing Director approval of a same-day batch whose hours have lapsed — server-clock only)**; BR-01/BR-02 amended, BR-04 explicitly unchanged (still per date). Data dictionary and §6.1 updated; Decision D-37. |
| 1.11 | July 18, 2026 | N. Medina | **Director analytics rescoped to system-collected data + dental kiosk linking.** (1) **Decision D-32** — the Medical Cases analytics (FR-ANL-02/03/06/08) and the entire case-category concept are dropped: HealthPass only ever sees clearance/kiosk encounters, never walk-in consultations, so a "Summary of Medical Cases" cannot represent the clinic's caseload and is indefensible at panel. Replaced by analytics over data the system itself captures — Clinic Visits by College (FR-ANL-09), Vital-Sign Flags with rates (FR-ANL-10), Visits per Month trend (FR-ANL-11), BMI Distribution (FR-ANL-12), and month + college page filters (FR-ANL-13); FR-ANL-04 (retitled "Students Screened by Sex", college-filtered) and FR-ANL-05 (Category column dropped) amended. **Supersedes D-23** — table #11 `clearance_case_categories` is removed (schema **11 → 10 tables**) — and amends the locked "Nurse encodes Fit/Unfit + case category" to **Fit/Unfit only**; D-30 and D-31 become moot. **FR-ANL-06 (export/print) is removed entirely** — analytics is on-screen only (no monthly print, no analytics CSV, no Flagged-Anomalies CSV). (2) **Decision D-33** — kiosk submit now links today's **dental** appointment (server-side, same trust rules), so the existing encode step completes dental appointments and dental visits gain a capture-time college snapshot; amends D-3. FR-ANL-02/03/06/08 superseded; FR-ANL-04/05/07 amended; FR-ANL-09..13 added; Decisions D-32, D-33. |
| 1.15 | July 28, 2026 | N. Medina | **Students are emailed when an appointment is created for them; batch appointments become non-student-cancellable.** **Decision D-39. No schema change.** (1) New **FR-STU-12** — a scheduling notice emailed once per appointment, for both creation paths: the student's own booking and, the main gap this closes, each appointment fanned out by a Director's batch approval, where the student previously had **no notification at all** that their college had booked them. The mail carries name, reference number, service, date, the D-37 one-hour slot, purpose, who booked it, clinic location, what to bring, a Kiosk Tutorial pointer and cancellation guidance — and **no clearance outcome, Fit/Unfit, vitals or questionnaire data** (FR-STU-08 unchanged). One Mailable + one **queued job per student**, dispatched **after the creating transaction commits** so a mail failure can never roll back appointments and a 60-student approval never blocks the Director's browser. Pre-D-37 appointments (`scheduled_time` NULL) render "—" and must not error. (2) **FR-STU-06 amended:** a `source` = 'batch' appointment is no longer student-cancellable — only the College Admin who booked the cohort may withdraw it, freeing the hour-slot. This was a **pre-existing gap found while writing the email**: `cancel()` guarded ownership, status and date but not source, so the email's "contact your college admin" would have been false. `Appointment::isSelfCancellable()` is now the single rule shared by the endpoint and both cancel-button views. (3) `healthpass.clinic_location` added as config. **Open item at the time of writing — the College Admin had no cancel/withdraw endpoint — closed by revision 1.16 / D-40.** |
| 1.16 | July 28, 2026 | N. Medina | **College Admins can withdraw a student's appointment from an approved batch.** **Decision D-40, new FR-ADM-07. No schema change.** Closes the gap revision 1.15 opened: D-39 told batch students to contact their college admin to cancel, but no such capability existed anywhere in the app — Batch Tracking showed only a student count, and there was no cancel route on the admin side at all. (1) New **batch roster page** (`GET /admin/batches/{batch}`), reached by clicking a batch reference on Batch Tracking: every student on the batch with, once approved, their appointment reference, one-hour slot and status. Before this there was no way to see who was in a batch. (2) New **withdraw** action (`DELETE /admin/batches/{batch}/appointments/{appointment}`) flipping one appointment to `cancelled`, which **frees its hour-seat automatically** because every capacity count already filters cancelled rows. The row is kept and the pivot link retained for audit. (3) Guards: `scheduled` status only (a `checked_in`/`completed` appointment may already back a `clinic_visits` row), not a past date, **today still allowed**; `Appointment::isAdminCancellable()` is the one rule shared by view and endpoint and re-checked under a row lock. College scoping follows FR-ADM-06 — foreign batch or appointment ids 404. |
| 1.17 | July 28, 2026 | N. Medina | **The withdrawn student is now emailed.** **Decision D-41, new FR-STU-13. No schema change.** Closes the last hole in the D-39 → D-40 chain: the student was emailed when their college booked them and cannot cancel a batch appointment themselves, but nothing told them when the College Admin withdrew it — they would have turned up to a seat that no longer existed. New `AppointmentWithdrawnMail` + `SendAppointmentWithdrawnMail`, queued and dispatched after the withdrawal transaction commits; a **refused** withdrawal emails nobody, and a **self-booked** cancellation deliberately sends nothing (the student did it themselves). The notice names the cancelled date, slot, service and reference, identifies the college, and **points the student at self-booking** so a withdrawal cannot silently become a lost clearance. Both notices now extend one abstract `App\Jobs\AppointmentMailJob` holding the retry policy, recipient resolution and PII-free failure logging, so those rules cannot drift between two near-identical job files. |
| 1.18 | August 18, 2026 | N. Medina | **Academic programs stop being free text.** **Decision D-42. No schema change.** From the adviser's review: the clinic must be able to report per academic program per college, which `student_profiles.course` — a plain text input validated only with `max:120` — could never support. The value set now comes from a new `config/programs.php` catalog keyed by college code (11 colleges; Senior High School is deliberately absent, D-43 removes it), read through `App\Support\Programs`. (1) Registration Step 2 and the My ID & Profile edit modal replace the Course text input with a **Program** select that is disabled until a College is chosen and repopulates when it changes; **Year Level is driven by the same per-college map** instead of a hardcoded 1–5 list, so Graduate Studies offers years 1–2 and Laboratory High School Grades 7–10. Alpine (already the house framework) drives the cascade; no package added. (2) **The server is the authority** — both Form Requests validate `course` and `year_level` with `Rule::in()` built from the college **actually submitted**, so a forged POST naming another college's program is rejected, and a missing or invalid college produces "Select your college first, then choose your program." rather than a bare "selected course is invalid". (3) The column keeps the name `course` (official form DHVSU-QSP-OSS-004-FO002-R03 prints "Course, Year & Section") and stays a string — **no table, no foreign key, no migration, no backfill**, so the 10-table canon is unchanged. FR-REG-03 and FR-STU-09 amended; Decision D-42. |
| 1.19 | August 18, 2026 | N. Medina | **Senior High School is removed; clinic visits snapshot the student's program.** **Decision D-43. FLAGGED SCHEMA CHANGE: `clinic_visits.course` VARCHAR(120) NULL.** (1) SHS is not on the PSU main-campus offerings, so the college list goes **12 → 11** — removed from `CollegeSeeder`, its administrator account from `StaffSeeder`, its 2 students from `StudentSeeder` (demo cohort 30 → 28), its visit weight from `DemoClinicVisitSeeder`, and from the college tables here and in `HealthPass_Context.md`. Seeded college admins drop 12 → 11. (2) `SubmitKioskVisit` now freezes the student's **program** on the visit beside the D-17 `college_id`, both read in one query from the server-side bound student; the column is **nullable and never backfilled**, so pre-D-43 visits render "—" the way pre-D-37 `scheduled_time` rows do, and a student with no program still submits (NULL) rather than being turned away at the kiosk. Without the snapshot, one program shift would silently restate every past per-program report. (3) Fixed a live bug found on the way: the seeder and factory wrote year-level **display labels** ('3rd Year', 'Grade 11') where the app stores and validates **keys** ('1'…'5', '7'…'10'), so every seeded student failed validation the moment they saved their own profile — `StudentProfileFactory` now reads `config/programs.php` instead of its own hand-written arrays, and a regression test covers both shapes. Nothing reads the new column yet; the reporting built on it comes later. FR-KSK-12 amended, §6.5 seed data amended; Decision D-43. |
| 1.20 | August 18, 2026 | N. Medina | **The nurse gets a dashboard, and it carries the encode log.** **Decision D-44. No schema change.** From the adviser's review: every log the system produces should be visible on the relevant role's dashboard. The nurse was the only role without a history surface — an encoded visit drops out of the Live Queue and was reachable only by URL, so a day's encoding could not be reviewed. New page `/nurse/dashboard` (**FR-NRS-09**, added): four stat tiles (encoded today, encoded this month, awaiting encode, flagged vitals this month) above a **clinic-wide** encode history built on `clearance_records`, newest first, 15 per page, filterable by month (reusing `App\Support\VisitMonths`) and result and searchable by student name or reference number, with the filters carried through the pager. Each row shows the visit's **D-43 program snapshot** ("—" when null) and the **nurse who encoded it** — the log is never scoped to the signed-in nurse, because two nurses on shift must read one continuous log. Rows reuse the existing read-only encode screen and reprint route; **no new detail page**. The dashboard becomes the nurse's **home** (`EnsureRole`), with Live Queue unchanged and still first-class in the nav. Decision D-44. |
| 1.21 | August 18, 2026 | N. Medina | **College Admins get per-program analytics; the analytics card builders become a shared service.** **Decision D-45, new FR-ADM-08. No schema change.** From the adviser's review: a college can request a monthly report of its own students' clinic results, which lands as analytics on the College Admin side rather than as a new report format. (1) The six card builders were **extracted** from `Director\AnalyticsController` (~390 lines) into **`App\Services\ClinicAnalytics`**, scoped at construction by month + optional college + optional program; the Director controller is now filter resolution and nothing else. The extraction's acceptance bar was `tests/Feature/Director/AnalyticsPageTest.php` passing **completely unedited**, which it did. (2) New page `GET /admin/analytics` (**FR-ADM-08**) with the same six cards, Clinic Visits by College replaced by **Clinic Visits by Program** (zero-visit programs included; medical reads the D-43 snapshot, dental the current profile; NULL snapshots collected in a "Not specified" row). Filters are month + program only — **the college is never read from the request**, so there is nothing for a `?college=` to override (FR-AUTH-06 / FR-ADM-06), and the program is validated against the *managed* college's catalog (D-42). (3) Two departures from a pure mirror, both for scope: the FR-ANL-11 trend stays inside the college (a clinic-wide aggregate is still other colleges' data), and the month picker offers only months this college has data in — `VisitMonths::available()`/`resolve()` gained an optional `?College` argument, Director and Nurse call sites untouched. (4) The Chart.js entry moved `resources/js/director/analytics.js` -> `resources/js/analytics.js` and its stacked-bar hook from `[data-college-bar]` to `[data-visits-bar]`, since both pages now use it. FR-ADM-08 added; FR-ANL-04/09..13 annotated with their new home; Decision D-45. |
| 1.22 | August 19, 2026 | N. Medina | **The College Admin's monthly report becomes a printable artifact; the Director's by-College card drops to programs when one college is selected.** **Decision D-46, new FR-ADM-09. No schema change, no new package, no CSV.** (1) New `GET /admin/analytics/print` (`Admin\MonthlyReportController`) renders `admin/monthly-report.blade.php` — a standalone Blade document, no app shell, loaded into a **hidden print frame** on the analytics page and printed in place, exactly the Module PRT pattern the clearance form uses. The frame + arming script were extracted to the shared `partials/print-frame`, and the **Nurse Dashboard's Reprint (FR-NRS-05) switched to it as well** — neither action opens a new tab any more, because the print dialog is the preview. It renders **tables, not charts**: header (university, college name + code, title, month in full, generated-at timestamp and the admin's name), summary line, Clinic Visits by Program with zero-visit programs and a totals row, Visits by Purpose, Vital-Sign Flags with counts + rates and the config-sourced threshold captions, BMI Distribution, Students Screened by Sex with percentages, and a footer stating the report covers **HealthPass-captured data only**. Permanently light (D-38). (2) **Same scope rule as FR-ADM-08** — the college is `managedCollege()`, `?college=` is never read; month and program carry over from the analytics page's query string, and a filtered report states its filter. Both pages draw on the same `App\Services\ClinicAnalytics`, so the printout cannot disagree with the screen. A **Print Monthly Report** button on `/admin/analytics` carries the current filters into the URL. (3) **FR-ANL-09 and FR-ANL-13 amended:** with the Director's college filter on a single college, the by-College chart is replaced by a by-Program chart for that college (`ClinicAnalytics::visitsByProgram()`, no new query logic); "All colleges" leaves the card unchanged. The existing Director analytics test needed **no edits to any existing assertion**. FR-ADM-09 added; FR-ANL-09/13 amended; Decision D-46. |
| 1.23 | August 19, 2026 | N. Medina | **The Director becomes the super admin — as a capability layer, not a fifth role — and provisions staff accounts in-app.** **Decision D-47, new FR-AUTH-10. NO schema change, no migration, no new package.** From the adviser's review: he asked whether the system has a super admin and assumed it was the Clinic Director. It did not have one, and the gap was operational — `college_admin` and `nurse` accounts could only be created by running `database/seeders/StaffSeeder.php`, which on the hosted deploy (D-34) means a developer with server and database access, with no in-app way to deactivate someone who has left or reissue a forgotten password. (1) New `GET /director/staff` (`Director\StaffAccountController`, `director/staff.blade.php`) inside the existing `['auth', 'role:director']` group, plus four writes — `POST /director/staff`, `POST /director/staff/{user}/password`, `PATCH /director/staff/{user}/status`, `PATCH /director/staff/{user}/college` — each with **its own throttle prefix**, since an authenticated inline throttle keys on the user id with no path. (2) **`users.role` is untouched**: FR-AUTH-02's four-value enum and D-2 stand, because super-admin here is *who may call five endpoints*, not a new role. (3) Credentials **reuse D-35 exactly** — `Str::password(16, symbols: false)`, `Hash::make`, `must_change_password = true`, `email_verified_at = now()`, and `RequirePasswordChange` forcing the change at first login; the plaintext is shown **once** in a show-once panel, never emailed, never persisted. (4) **Anti-escalation is server-side in three places** — the role whitelist lives in `StoreStaffAccountRequest` (a posted `role=director` or `role=student` is a validation failure, asserted by test), `guardManageable()` refuses any target that is not a `college_admin`/`nurse`, and it refuses the Director's own row so they cannot deactivate or re-credential themselves. A `college_admin` can be neither created nor left without a `managed_college_id` (FR-AUTH-06). (5) **Nothing is ever deleted** — `clearance_records.encoded_by` / `batch_requests.reviewed_by` are `restrictOnDelete`; deactivation is the only removal and history is untouched, asserted by test. (6) **Deactivation bites on the next request, not the next login** (amends **FR-AUTH-07**): `users.status` was checked only by `LoginRequest`, so before this a revoked account kept working for the rest of `SESSION_LIFETIME`. New `App\Http\Middleware\EnsureAccountIsActive` on the web group, ahead of `RequirePasswordChange`, ends the session and returns them to login with the reason (JSON gets 403). `StaffSeeder` is **unchanged** and remains the bootstrap for the first Director account. FR-AUTH-10 added; FR-AUTH-05 annotated; Decision D-47. |
| 1.24 | August 19, 2026 | N. Medina | **Review pass on the Staff Accounts screen: the password-reset action is removed, college reassignment becomes confirm-on-change, and a "Last active" column is added.** **Decisions D-47a and D-48. FLAGGED SCHEMA CHANGE: `users.last_active_at` TIMESTAMP NULL** (nullable, no default, never backfilled). (1) **D-47a — the Director's "Reset password" button, its `POST /director/staff/{user}/password` route and its controller method are GONE.** A Director able to set an existing account's password can sign in as that person and read their medical records, which is exactly the impersonation D-47's own out-of-scope list rules out; the button was a supported path to it. Password generation now happens in one place only, `StaffAccountController::store()`, and a staff member who forgets theirs uses the FR-AUTH-09 forgot-password OTP like every other role. Tests assert the route no longer exists and the page offers no such control. (2) **D-47a — college reassignment is confirm-on-change, through the app's own modal.** The Save button beside the dropdown read as an unclear second step; selecting a college now opens a confirmation dialog and submits on accept, restoring the previous selection on cancel, Esc or a backdrop click, so the control can never display a college the admin is not assigned to. The dialog reuses the **same teleported Alpine panel as `x-logout-confirm`** (new `components/college-reassign-confirm.blade.php`) rather than a native `window.confirm()`, which was unstyled and out of keeping with every other confirmation in the app. The target college name is read at runtime from the option's `data-name` and never interpolated into a JavaScript string — an apostrophe would break the literal and silently skip the guard (the trap already documented on kiosk-devices Revoke). `x-hp.select` gained `pl-3 pr-9`: equal padding let a long option label run under the browser's dropdown arrow and hide it. (3) **D-48 — new "Last active" column** beside Status, because `users.status` answers "is this account allowed in", not "is anyone still using it", and a College Admin who left in June reads as Active until somebody notices. Stamped by new `App\Http\Middleware\RecordLastActive`, appended to the web group **after** `EnsureAccountIsActive` and `RequirePasswordChange` so a request those gates turn away does not count as activity; **throttled to one write per account per minute**, and written with the query builder because an Eloquent `update()` would silently bump `updated_at` into a second last-seen stamp (caught by test). Never-seen accounts render **"Never"**. FR-AUTH-10 amended; `users` data dictionary updated; Decisions D-47a, D-48. |
| 1.25 | August 19, 2026 | N. Medina | **College Admins get an Activity Log, and it is derived rather than logged.** **Decision D-49, new FR-ADM-10. NO schema change and NO new table.** The question behind it came from Nat after FR-AUTH-10 shipped: now that the Director can create a second College Admin in two minutes, can two admins step on each other? They could - neither could see the other's submissions, and the Director's approvals and rejections left no trace anywhere a college could read as history. New `GET /admin/activity` (`Admin\ActivityLogController`, `admin/activity.blade.php`) plus an **Activity Log** sidebar entry, showing each batch **submission** (author, time, student count, reason) and each **Director decision** (approved with the generated date, or rejected with the written D-36 reason), newest first, 15 per page. **Every entry is read back out of `batch_requests`** - there is no audit table and nothing writes a log row, so the page cannot drift from what Batch Tracking shows. An `activity_logs` table was considered and **rejected**: an 11th table in a 10-table canon, a write path on every action that a later edit can forget, and a record that can disagree with the data it describes. Flattening is done in PHP rather than a SQL UNION, since a UNION means raw SQL and the suite runs on SQLite. Scope is `managedCollege()` with `?college=` never read, asserted by test. Excluded deliberately: appointment withdrawals (no `cancelled_by` column exists to name the actor), student self-bookings (would bury the admin actions), reads/page views, and anything clinical. FR-ADM-10 added; Decision D-49. |

---

## 1. Overview

### 1.1 Product summary

HealthPass is a **single, unified Laravel web application** that runs Pampanga State University's campus-clinic **medical and dental clearance process end-to-end**, paired with a **self-service kiosk** (a Blade route within the same application, displayed full-screen on a Raspberry Pi) that captures student vital signs and a nine-item body-system screening questionnaire.

The system covers the complete lifecycle: students register and link a QR-coded ID; they book appointments themselves or are batch-enrolled by their College Admin (subject to Clinic Director approval); on arrival they use the kiosk to record vitals and screening answers; a nurse reviews each submission in a live queue, encodes the clinical result (Fit/Unfit plus case category), and prints the official PamSU Medical Clearance form; and the Clinic Director monitors approvals, analytics, and flagged anomalies.

**There is no artificial intelligence in this system.** All vital-sign flagging is rule-based against fixed, documented thresholds (§7.4). All clinical judgment is made by clinic staff. The system schedules, captures, records, flags by rule, and prints.

### 1.2 Problem statement

The campus clinic's medical and dental clearance process is currently manual: students queue physically without scheduled slots, personal and clinical information is handwritten and re-transcribed across forms and logbooks, colleges requesting clearances for entire groups (e.g., OJT batches, graduating classes) coordinate by paper lists, and the clinic has no consolidated view of case categories, volumes, or anomalous readings across colleges. This produces long waits during peak clearance seasons, transcription errors, lost or duplicated records, and zero analytical visibility for clinic management.

HealthPass replaces this with scheduled appointments, one-time digital registration, self-service vitals capture at a kiosk, a structured nurse workflow that ends in a printed official clearance form identical to the existing paper form, and a Director dashboard summarizing medical cases by category, college, and sex.

### 1.3 Background and prior scope (what changed)

Earlier iterations of this capstone proposed an *AI-Powered Health Screening Kiosk with Predictive Risk Profiling* (Azure OpenAI GPT-4o, pseudonymization, predictive analytics). That scope has been **fully dropped**. The current system is purely scheduling + digital clearance with rule-based flagging.

> **Open documentation action:** the capstone paper still carries the AI-era title and scope. The documentation team must revise the title, objectives, and chapter scope so the defended paper matches this PRD. This is a parallel workstream with its own deadline (§15, Risk R-6).

### 1.4 Goals

1. **G1 — Digitize the clearance loop end-to-end:** from booking to printed official form, with no paper handoffs except the final printed clearance itself.
2. **G2 — Reduce per-student processing effort:** self-service kiosk capture replaces manual measurement transcription; the nurse encodes from a pre-populated screen.
3. **G3 — Give the Clinic Director operational visibility:** approvals, case-category analytics by college and sex, and rule-flagged anomalies in one dashboard.
4. **G4 — Enforce data privacy by design:** RA 10173-compliant consent capture at registration and at every kiosk session, role-based access control, server-side college scoping, no health data leaving the server.
5. **G5 — Deliver a defensible working prototype by August 30, 2026** that passes the team's ISO/IEC 25010:2023 evaluation instrument.

### 1.5 Success metrics (measurable)

| ID | Metric | Target | Verification |
|---|---|---|---|
| SM-1 | Complete kiosk session (QR login → submit) | ≤ 5 minutes per student | Timed UAT runs (W11) |
| SM-2 | New kiosk submission appears in nurse Live Queue | ≤ 5 seconds (one polling cycle) | Stopwatch test during integration |
| SM-3 | Printed clearance vs. official form DHVSU-QSP-OSS-004-FO002-R03 | Field-for-field identical layout | Side-by-side print comparison signed off by clinic staff |
| SM-4 | Captured visits retained if encoding is delayed | 100% (zero loss) | Submit N visits, restart services, verify all N remain `captured` |
| SM-5 | Role isolation | 0 cross-role or cross-college data leaks | Negative-path test cases (College Admin A cannot see College B; student cannot reach `/nurse/*`) |
| SM-6 | Rule flags correctness | 100% agreement with §7.4 thresholds on a seeded test set | Automated/manual test against boundary values |
| SM-7 | End-to-end happy path | Book → kiosk → encode → print completes without developer intervention | Full dress rehearsal (W11–W12) |
| SM-8 | ISO/IEC 25010 evaluation | Acceptable rating per the team's instrument | Evaluation runs in W11 |

### 1.6 Non-goals

Explicitly **out of scope** for this release:

- AI / machine learning / predictive risk profiling of any kind
- A Doctor role, doctor login, or teleconsultation (the University Physician's name and license number are pre-printed on the form only)
- Payments, pharmacy, medicine inventory
- Laboratory results, imaging, or referral management
- Native mobile applications (the web app is responsive desktop-first; the kiosk targets a fixed 1080×1920 portrait display, D-26)
- Faculty and NASA (non-academic staff) clearances — analytics and clearance records cover students only; Faculty and NASA visits are explicitly out of scope and excluded from all analytics
- SMS notifications
- Multi-clinic or multi-campus support (single PamSU campus clinic, one kiosk)
- Real-time WebSockets (queue refresh is polling-based by design)

---

## 2. Users and Personas

Only **students self-register**. `nurse`, `college_admin`, and `director` accounts are seeded or provisioned by the team — there is no public staff registration.

### 2.1 Student

A PamSU student (any of the 11 colleges, D-43) who needs a Medical Clearance (graduation, OJT, sports, field trip, etc.) or a Dental Check. Uses the web app from a personal device to register, book, and view records; uses the kiosk on-site with a QR-coded ID. Technical skill: basic smartphone/web literacy. Key needs: book without queuing blind, finish the kiosk quickly, see their own history.

### 2.2 College Admin

A designated staff member of **exactly one college** (e.g., the CCS admin). Submits batch clearance requests on behalf of groups of students (a graduating block, an OJT cohort) and tracks their status. **Hard constraint:** every screen, list, and query is scoped to their `managed_college_id`, enforced server-side; the admin can never change or escape this scope. Key needs: select many students fast, know batch status at a glance.

### 2.3 Nurse

The clinic nurse operating the day-to-day flow. Watches the Live Queue of kiosk submissions, opens each visit, reviews vitals (with rule flags) and questionnaire answers, encodes the result (Fit/Unfit + case category + purpose + notes), and prints the official clearance. Also launches kiosk mode on the clinic terminal. The encode screen is titled **"Doctor's Assessment"** in the UI but is nurse-operated (Decision D-2). Key needs: see new arrivals immediately, encode in under a minute, print that matches the official form exactly.

### 2.4 Clinic Director

Head of the Health Services Unit. Approves or rejects college batch requests, monitors KPIs, reviews **clinic-activity analytics** — visits by college and service type, vital-sign flag rates, and monthly trends (rescoped by D-32 from the former Summary of Medical Cases) — and the **Flagged Anomalies** list. Key needs: act on pending batches quickly, get a defensible statistical picture from the system's own data for reporting.

### 2.5 Colleges (reference data)

| Code | Full name |
|---|---|
| COE  | College of Education |
| CEA  | College of Architecture and Engineering |
| CBS  | College of Business Studies |
| CAS  | College of Arts and Science |
| CSSP | College of Social Science and Philosophy |
| CCS  | College of Computing Studies |
| CHTM | College of Hospitality and Management |
| CIT  | College of Industrial Technology |
| LAW  | School of Law |
| GS   | Graduate Studies |
| LHS  | Laboratory High School |

**11 units since D-43** (was 12). Senior High School was removed: it is not on
the PSU main-campus (bicolor) program offerings, so it is not a unit HealthPass
serves.

Each college has **exactly one** College Admin account.

---

## 3. End-to-End Workflow (Clearance Lifecycle)

```
Student registers → consents (RA 10173) → links ID QR
         │
         ├── Path A: self-books an appointment (Medical or Dental)
         │
         └── Path B: College Admin submits a batch request (with a
                     requested clinic date — Decision D-29)
                        └── Director confirms → system auto-creates one
                            appointment per listed student ON THE
                            REQUESTED DATE (approval is confirm-only;
                            the Director cannot move the date —
                            Decisions D-5/D-29/D-36)
                        └── …or rejects with a written reason, which the
                            College Admin reads on Batch Tracking and
                            acts on by resubmitting (D-36)

Student arrives at clinic (booked or walk-in)
         │
         └── Kiosk login (QR scan via USB scanner OR email + virtual keyboard)
                │
                └── Identity Confirm → Walk-in check (FR-KSK-03a):
                │     no non-cancelled appointment dated today (medical OR dental)
                │     → "No Scheduled Clearance Today" → Proceed as Walk-in
                │       (or exit to Welcome); any same-day appointment skips the
                │       notice. Dental suppresses the notice but never links at
                │       submit — it stays scheduling-only (Decision D-3).
                │
                └── Privacy consent (RA 10173) — decline resets to Welcome
                │
                └── Vitals: Height → Weight (+ auto BMI) → Temperature → BP (+ Heart Rate)
                │     (sensor capture with manual-entry override on every step — Decision D-7)
                │
                └── 9-item body-system questionnaire + pregnancy/LMP
                │
                └── Review & Submit → Clinic Visit created (status: captured)
                        (links to today's MEDICAL appointment if one exists, else walk-in)

Nurse sees the visit in the Live Queue (polling, ≤ 5 s)
         │
         └── Opens Encode Result ("Doctor's Assessment")
                │  reviews vitals with flags + questionnaire answers
                └── Sets Fit/Unfit + case category + purpose + notes
                └── Preview & Print clearance (official PamSU form)
                └── Save & Close → visit status: encoded; Clearance Record created

Director analytics and Flagged Anomalies update from encoded records
```

**Dental appointments are scheduling-only** (Decision D-3): a dental booking creates an appointment and appears in records, but does not go through the kiosk vitals → encode → print loop.

---
## 4. Functional Requirements

Notation: each requirement has an ID, a MoSCoW priority — **M**ust (defense-critical), **S**hould (expected, slip candidate only under pressure), **C**ould (nice-to-have) — and acceptance criteria (AC). "The system" = the unified Laravel app.

### 4.1 Module AUTH — Authentication, Roles & Access Control

| ID | Requirement | Priority |
|---|---|---|
| FR-AUTH-01 | The system shall authenticate users by email + password via Laravel Breeze (Blade stack), with hashed (bcrypt) password storage. | M |
| FR-AUTH-02 | The system shall support exactly four roles: `student`, `college_admin`, `nurse`, `director`, stored on `users.role`. | M |
| FR-AUTH-03 | Role middleware shall scope all route groups: `/student/*`, `/admin/*`, `/nurse/*`, `/director/*`. A user requesting a route outside their role receives HTTP 403 (or redirect to their own dashboard). | M |
| FR-AUTH-04 | After login, the system shall redirect each role to its own dashboard (Student Dashboard, Admin Dashboard, Live Queue, Director Dashboard respectively). | M |
| FR-AUTH-05 | Staff accounts (`nurse`, `college_admin`, `director`) shall be created only via database seeders / provisioning — no public staff registration path shall exist. *(D-47: **in-app provisioning by the Director (FR-AUTH-10) satisfies the "provisioning" clause as written** — it is an authenticated, `role:director`-gated screen. **No public staff registration path is added**, and none may be: self-registration with an approval queue was considered and rejected. The seeder remains the bootstrap path for the first Director account.)* | M |
| FR-AUTH-06 | College Admin accounts shall carry a non-null `managed_college_id`; **all** Admin queries shall be filtered by this value server-side. The value shall never be settable through any Admin-facing request. | M |
| FR-AUTH-07 | The system shall support an `active`/`inactive` user status; inactive users cannot log in. *(D-47: **deactivation also ends a session that is already open.** `users.status` is re-checked on every web request by `App\Http\Middleware\EnsureAccountIsActive`, which logs the user out, invalidates the session and returns them to the login page with the reason — a JSON request gets 403 instead, so a background poll is never handed an HTML login page. Login-time refusal alone left a revoked account working for the remainder of `SESSION_LIFETIME`.)* | S |
| FR-AUTH-08 | The login **page** shall use a **two-panel layout**: the login **card** on the left (the existing 420px column — HPLogo + tagline, email + password with eye toggle, "Register here" link, RA 10173 footer note; **card internals unchanged**) and a decorative illustration panel on the right. The illustration is shown **whole and centered** (contained, never cropped) on an off-white panel that matches its backdrop. On narrow screens the layout **collapses back to the centered single-column card** (illustration panel hidden). Visual source of truth for the login page: the mockup in `docs/prototypes/web/`. | S |
| FR-AUTH-09 | Session handling and CSRF protection shall use Laravel/Breeze defaults. *(Amended per D-20 — pending adviser sign-off:)* Password recovery and password change shall use the D-8 OTP pattern instead of Breeze's emailed reset link: **(a) Forgot password (guest)** — email → 6-digit OTP → new password, with a generic "if an account exists" response that never reveals whether an email is registered; **(b) Change password (authenticated, all four roles)** — current + new password staged server-side, applied only after an OTP sent to the account email is confirmed. In both flows the password changes only on the final confirmed submit; abandoning leaves it untouched. A successful change regenerates the session and logs out the user's other sessions. Every OTP screen enforces a **60-second resend cooldown** server-side (`RESEND_COOLDOWN_SECONDS`), with the countdown driven by a server-provided timestamp. | M |
| FR-AUTH-10 | **Staff Account Provisioning (D-47).** The Director shall be able to create `college_admin` and `nurse` accounts **in-app** (`GET/POST /director/staff`), issuing a **one-time password shown once** and requiring a change at first login; to **activate or deactivate** an account (`PATCH /director/staff/{user}/status`); and to **reassign a College Admin's managed college** (`PATCH /director/staff/{user}/college`). The Director **shall not** be able to provision a `director` or `student` account, nor deactivate their own account, **nor set the password of an existing account** — the joining password is issued once, at creation, and a staff member who forgets theirs recovers it through the FR-AUTH-09 forgot-password OTP, which only their own mailbox can complete (D-47a). The screen shall additionally show, per account, **when it was last active** (`users.last_active_at`, D-48), rendering **"Never"** for an account not seen since that column existed. Extends FR-AUTH-05 (provisioning) and FR-AUTH-07 (status) rather than replacing either. Credentials reuse the D-35 flow exactly — `Str::password(16, symbols: false)`, `Hash::make`, `must_change_password = true`, `RequirePasswordChange` — and the plaintext is **flashed to one request and stored nowhere**. The role whitelist is enforced **server-side in `StoreStaffAccountRequest`**, not merely absent from the dropdown, and every write endpoint additionally refuses a target that is the signed-in Director or is not a `college_admin`/`nurse`. A `college_admin` may never be created or left with a null `managed_college_id` (FR-AUTH-06 would 403 them on every Admin route). **Accounts are never deleted** — `clearance_records.encoded_by` and `batch_requests.reviewed_by` are `restrictOnDelete`, so deactivation is the only removal, and it leaves every historical record intact. | S |

**AC (module):** logging in as each seeded role lands on the correct dashboard; a student manually entering `/nurse/queue` is rejected; the CCS admin's student list returns only CCS students even when query parameters are tampered with; an inactive account is refused with a clear message.

### 4.2 Module REG — Student Self-Registration (4-step wizard)

| ID | Requirement | Priority |
|---|---|---|
| FR-REG-01 | Registration shall be a 4-step wizard with a top progress bar: **Consent → Account Info → Email Verify → Link ID**. | M |
| FR-REG-02 | **Step 1 (Consent):** display the RA 10173 data-privacy notice; the Continue button shall remain disabled until the consent checkbox is ticked. Consent timestamp is stored in `student_profiles.privacy_consent_at`. | M |
| FR-REG-03 | **Step 2 (Personal Information):** capture First Name, Middle Name (optional), Last Name, Student Number (unique), College (dropdown of the 11 colleges, D-43), Sex (M/F), Course & Year, Date of Birth (with auto-computed Age badge), Place of Birth, Civil Status (Single/Married/Widowed/Separated), Address, Email (unique), Password. All fields validated server-side. **Amended by D-42 — Program (the `course` field, labelled "Program" in the UI) and Year Level are college-dependent dropdowns sourced from `config/programs.php`, not free text: the Program select is disabled until a College is chosen and repopulates when it changes, and Year Level is driven by the same per-college map (so Laboratory High School offers Grades 7–10, Graduate Studies years 1–2, Architecture and Engineering years 1–5). The cascade is UX only — the server re-validates both against the catalog entry for the college **actually submitted**, so a POST naming another college's program is rejected.** | M |
| FR-REG-04 | **Step 3 (Email Verify):** the system shall verify email ownership via a 6-digit OTP entered in six auto-advancing boxes, with a Resend link; Verify & Continue is disabled until 6 digits are entered (Decision D-8: OTP is generated server-side, stored **hashed in cache with a TTL of 10 minutes** — no database table; mail driver is `log`/Mailtrap in dev, SMTP in production). | M |
| FR-REG-05 | OTP attempts shall be rate-limited (max 5 verify attempts per code; resend invalidates the previous code). | S |
| FR-REG-06 | **Step 4 (Link ID):** the student may link their physical ID by capturing its QR **in-browser** on their own device — either (a) pointing the device camera at the back of the physical DHVSU ID, or (b) uploading a photo of it — decoded client-side by `html5-qrcode`. The `IDNo` line is extracted from the multi-line QR payload; non-digit characters are stripped from both the extracted IDNo and the `student_number` entered in Step 2, then compared — a mismatch produces a clear error and no binding occurs. On a valid match, only the IDNo value is POSTed to the server, replacing the provisional `qr_token` generated at Step 3 (`qr_token` stays NOT NULL throughout). The student may also press "Skip for now" and link later from the My ID screen. There is **no USB scanner at registration** — students register on personal devices. | M |
| FR-REG-07 | On completing (or skipping) Step 4 the system shall log the student in and route to the Student Dashboard. | M |
| FR-REG-08 | A registration abandoned before Step 3 completion shall not create a verified, login-capable account. | M |

**AC:** a new student can complete all 4 steps in one sitting; the OTP in `storage/logs/laravel.log` (dev) verifies successfully; a wrong OTP 5× invalidates the code; skipping QR still produces a working account (provisional `qr_token` retained); duplicate student number or email is rejected with a field-level error; an IDNo that does not match `student_number` (digit-normalized) is rejected with a clear error; a successful QR capture replaces the provisional `qr_token` with the IDNo; an IDNo already bound to another student is rejected.

### 4.3 Module STU — Student Portal

| ID | Requirement | Priority |
|---|---|---|
| FR-STU-01 | **Dashboard** shall show three stat cards — Clearance Status (latest result badge + "Book New Appointment" button), Next Appointment (nearest upcoming non-cancelled appointment, date + service + clinic hours, with a "Cancel appointment" ghost action that opens a confirm modal), Past Clearances (count + "View all") — plus a Recent Activity timeline of the student's own events. | M |
| FR-STU-02 | **Book Appointment** shall present a service picker (Medical Clearance / Dental Check as selectable cards), a month calendar grid in which past dates are disabled and full days are greyed with a "FULL" label, and — for **Medical Clearance only** — a **Purpose of Medical Clearance** card (the same locked dropdown as nurse encode: the four PRD purposes plus **Others, Specify** free-text; D-28). The purpose card is hidden for Dental (scheduling-only, no clearance form). | M |
| FR-STU-03 | Day capacity shall be evaluated as: count of non-cancelled appointments on that date ≥ configured capacity (`config/healthpass.php`, Decision D-4) → day is FULL. **(D-37)** A date shall **additionally** be FULL when **every** one of its ten one-hour slots has reached `hourly_capacity` (BR-21) **or, on today, has already elapsed (BR-23)**, and shall be greyed out in the calendar exactly as a capacity-full day is; a date with even one free, unelapsed hour remains bookable. After the student picks a date, the availability endpoint (`GET /student/appointments/availability`, extended — **not** a second route) shall additionally return that date's per-slot `booked` / `remaining` / `full` / **`elapsed` / `available` (BR-23)** — `full` and `elapsed` are reported separately so the picker can say *why* an hour is unavailable ("Full" vs "Past"). The month roll-up shall be computed with one grouped query over the raw `(scheduled_date, scheduled_time)` pair and rolled up in PHP — **no MySQL-only date functions**, so the SQLite test suite exercises the same SQL. | M |
| FR-STU-04 | **(D-37)** After picking a date the student shall pick a **one-hour time slot**; each slot shall show its remaining capacity and a slot shall be disabled when it is at `hourly_capacity` (labelled "Full") **or, on today, has already ended (labelled "Past" — BR-23)**. A selected hour that becomes unavailable while the page is open shall be cleared rather than submitted. The chosen slot is stored in `appointments.scheduled_time` and is **required** for every new booking. Capacity for that slot shall be re-checked under `lockForUpdate()` **inside the same transaction as the insert** — enforcement shall not live in the Form Request alone (BR-21). Confirm Booking shall be disabled until a service, a date **and a time slot** are selected **and — for Medical Clearance — a purpose is chosen** (Others additionally requires its specify-event text, max 120 chars; validated server-side in the booking Form Request, never trusted from the client — D-28); on click a confirmation modal ("Book {Service} on {date} at {time}?" → Yes, book it / Cancel) shall appear before the record is created; on confirmation the system creates an `appointments` row (`source = self`, status `scheduled`, reference `APT-YYYY-####`, and — for medical — the chosen `purpose`/`purpose_other`; purpose is nulled for dental) and routes to a confirmation screen showing Service, Date, **Time slot**, Clinic Hours, and Reference No. The student-chosen purpose is carried through to the nurse encode step and the printed form (FR-NRS-03, FR-PRT-02). | M |
| FR-STU-05 | The system shall reject (validation error, not DB constraint) a booking when the student already has a non-cancelled appointment for the same service on the same date; the duplicate rejection shall be presented as an in-page modal with a "Choose another date" action — no page reload, selected service and date preserved. | M |
| FR-STU-06 | Students may cancel their own **self-booked** (`source` = 'self') `scheduled` appointments before the appointment date; cancellation is reachable from the dashboard Next Appointment card. **(D-39)** A **batch-generated** appointment (`source` = 'batch') is **not** student-cancellable: it belongs to the cohort the College Admin booked, so only that admin may withdraw it, and a cancelled seat frees its hour-slot for another student. The dashboard and confirmation views shall hide the cancel control for such appointments and show "Booked by your college — contact your college administrator" instead, and the endpoint shall refuse them with 403; both read the single rule `Appointment::isSelfCancellable()` so the button and the server can never disagree. | S |
| FR-STU-07 | **My Records** shall list the student's clinic visits/clearances (Date, Service, Result badge, Reference No.) with a View action opening a record modal: left column = kiosk vitals (height, weight, BMI, temp, HR, BP) + case category badge if present; right column = the 9 questionnaire answers as Yes/No badges (Yes = flagged style). | M |
| FR-STU-08 | The student shall **never** see the Fit/Unfit determination on the kiosk; results become visible in My Records only after nurse encoding. | M |
| FR-STU-09 | **My ID & Profile** shall show the student's kiosk QR code (generated via `simplesoftwareio/simple-qrcode` from `qr_token`) with an Active badge, plus read-only profile fields, and an Edit Profile modal for: name, email, course, year, **college** (students transfer colleges — self-editable), address, DOB, place of birth, civil status. Student number and sex are not self-editable. Changing **college** immediately re-scopes the student to the new College Admin (they leave the prior admin's student list and batch eligibility and appear under the new one); already-encoded `clearance_records` keep the college recorded at capture time (snapshot on `clinic_visits.college_id`, §6.2), so analytics history does not move. **Amended by D-42 — the Edit modal uses the same college-dependent cascade as FR-REG-03: changing College re-scopes the Program list, clearing a program the new college does not offer rather than keeping a now-invalid value. A transfer that keeps the old college's program is rejected server-side (`UpdateStudentProfileRequest` validates both Program and Year Level against the submitted college).** | M |
| FR-STU-10 | If the student skipped QR linking at registration, the My ID screen shall offer the same in-browser capture flow used in Step 4: camera or uploaded ID photo decoded by `html5-qrcode`, `IDNo` extracted, IDNo↔`student_number` match verified, then bound as `qr_token`. | M |
| FR-STU-11 | **Kiosk Tutorial** (web app, student nav — *not* on the kiosk): a single page walking the student through the kiosk flow in 7 client-side steps (one Alpine state object, no per-step routes) — landing, approach & QR/email login, height, weight & BMI, temperature, blood pressure & pulse rate, finishing up (review/submit → proceed to the clinic nurse; consistent with FR-STU-08, the tutorial states the kiosk never shows a result). Steps 2–7 share a media-plus-instructions layout; **the media slots are styled GIF placeholders pending real hardware footage** (to be recorded by the hardware track). Additionally, the booking confirmation screen (FR-STU-04) shall show a dismissible modal recommending the tutorial when the appointment just booked is the student's **first ever** (total appointment count, any status, == 1 — computed server-side). | S |

| FR-STU-12 | **Appointment scheduling notice by email (D-39).** The system shall email the student **once per appointment created**, for both sources: their own booking (FR-STU-04) and each appointment fanned out by a Director's batch approval (FR-DIRA-02 / BR-08) — the latter being the case where the student currently has *no* way of learning their college booked them. The mail shall contain, and shall be readable without logging in: the student's name; the reference number (`APT-YYYY-####`); service type (Medical/Dental); the scheduled **date** and the one-hour **time slot** (D-37; a pre-D-37 appointment with `scheduled_time` NULL renders "—" and must not error); the purpose/reason (the student's `purpose`/`purpose_other` when self-booked, the batch's `reasonText()` when batch-generated); whether it was self-booked or booked by the college (named); the clinic location (`healthpass.clinic_location`) and what to bring; a pointer to the Kiosk Tutorial (FR-STU-11); and cancellation guidance consistent with FR-STU-06 — self-booked students cancel from their dashboard, batch students are directed to their College Admin. **It shall contain no clearance outcome, Fit/Unfit status, vitals or questionnaire data** (FR-STU-08). Delivery shall be **queued, one job per student**, dispatched **only after** the creating transaction has committed, so that neither an SMTP failure nor a slow provider can roll back or block a booking or a 60-student approval, and a single bad address loses only its own message. Free-text fields (`purpose_other`, `reason_detail`, college name) shall be escaped in the template. Failures shall be logged with the appointment reference and student id **only** — no name, address or clinical detail. | M |

| FR-STU-13 | **Appointment withdrawal notice by email (D-41).** When a College Admin withdraws a student's appointment from an approved batch (FR-ADM-07), the system shall email **that one student** — and only that student — telling them the appointment is cancelled and they need not attend. The mail shall name the cancelled date, one-hour slot, service and reference number (a pre-D-37 appointment with `scheduled_time` NULL renders "—" and must not error), identify the college that withdrew it, point the student at self-booking if they still need clearance (a withdrawn student is an ordinary student again and self-booking is open to them — omitted once the clinic date has passed), and give them a route back to their college administrator if the withdrawal was a mistake. **It shall contain no clearance outcome, Fit/Unfit status, vitals or questionnaire data** (FR-STU-08). Delivery shall be **queued and dispatched only after the withdrawal transaction commits**, on the same terms as FR-STU-12, so a mail failure can never roll back the withdrawal. A **refused** withdrawal (already checked in, completed, past-dated, or a duplicate submit) shall email nobody. Self-booked cancellations send **no** email — the student performed that action themselves and needs no notice of it. | M |

**AC:** booking a full day is impossible from the UI and rejected server-side if forced; the calendar never allows a Saturday/Sunday or yesterday; a fresh student sees an empty-state dashboard without errors; the QR rendered on My ID scans back to the same `qr_token` at the kiosk; approving a batch of N students queues exactly N appointment emails addressed to those N students and carrying each student's own hour, while a rejected or rolled-back approval queues none.

### 4.4 Module ADM — College Admin Batch Workflow

| ID | Requirement | Priority |
|---|---|---|
| FR-ADM-01 | **Admin Dashboard** shall display a college-scope banner ("{College Full Name} — you can only manage students and batch requests for your assigned college"), four stat cards (Registered Students in college, Total Batches, Pending Approval, Approved), and a table of the college's batch requests. | S |
| FR-ADM-02 | **New Batch Request** shall require: a Reason (dropdown: graduation clearance, OJT/practicum, general enrollment, scholarship, sports/athletics, field trip/educational tour, others), a "Please specify" textarea **required iff** reason = others, a Service Type (medical/dental), a **Requested Clinic Date** (date picker, defaulting to today; past dates disallowed — D-29), and ≥ 1 student selected. | M |
| FR-ADM-03 | The student multi-select shall list **only** students of the admin's college, searchable by name or student number, with Select All / Clear, peach-highlighted selected rows, and a "(N of M selected)" counter. Submit is disabled until valid. | M |
| FR-ADM-04 | **(D-37)** The form shall additionally require a **start hour** chosen from the ten clinic slots. The system shall compute the span as `ceil(students ÷ 12)` **contiguous** hour-blocks and display it back live — e.g. *"30 students → 7:00 AM – 10:00 AM (3 slots)"*. Submission shall be **blocked** when any block in the computed span is already at the per-hour cap (**the error names the offending hour**, not just "full"), **when the requested date is today and the chosen start hour has already ended (BR-23 — elapsed hours are disabled in the select, from a server-computed list, and refused server-side)**, when the span would run past 5:00 PM, or when the roster exceeds 120 students (which cannot fit in one clinic day — the message shall say so and suggest splitting across two dates). These checks run in `StoreBatchRequestRequest` **and are re-checked under `lockForUpdate()` at write time**, same reasoning as FR-STU-04. The span **length is always derived server-side** from the roster; a `requested_blocks` posted by a client is ignored. On submit the system creates a `batch_requests` row (status `pending`, reference `BR-YYYY-###`, `requested_date` = the admin's chosen date — D-29, `requested_time` = the chosen start hour and `requested_blocks` = the computed span length — D-37) plus one `batch_request_students` row per selected student, then shows a confirmation screen (Batch ID, "Pending Director Approval", requested date, **requested time span**, submitted date) with View Tracking / Dashboard actions. | M |
| FR-ADM-05 | **Batch Tracking** shall list the college's requests (Batch ID, Reason truncated, student count, Submitted date, Status — pending displayed as "Pending Director Approval"). **A Rejection Reason column (D-36) shall appear beside Status only when the list contains at least one rejected row**; on a rejected row it renders a View button opening a modal with the Director's written reason, the reviewing Director's name and `reviewed_at`, and on every other row an em dash. The reason is Director-supplied free text and shall be **escaped on output**. Both renderings of the list — the desktop table and the mobile stacked cards — carry the column. | M |
| FR-ADM-06 | Server-side enforcement: any attempt to include a student from another college, or to read another college's batch, shall fail regardless of client-side state. | M |

| FR-ADM-07 | **Batch roster + appointment withdrawal (D-40).** Each row in Batch Tracking shall link to a **batch roster page** listing every student on that batch and — once approved — the appointment generated for them (reference number, one-hour slot, status). On an **approved** batch the admin may **withdraw one student's appointment**, which sets that appointment's status to `cancelled` and thereby frees its hour-seat for another student (every capacity count filters `status != 'cancelled'`, so no separate bookkeeping is required). This is the counterpart of FR-STU-06's D-39 restriction: batch students cannot cancel their own appointment and their FR-STU-12 email directs them to their College Admin, so the admin must have this power. The appointment row shall be **flipped, never deleted**, and its `batch_request_students.appointment_id` link retained, so the batch's history remains readable. Withdrawal shall be refused (and the control hidden) when the appointment is not `scheduled` — a `checked_in` or `completed` student is at or through the kiosk and may already have a `clinic_visits` row pointing at it — or when its date has already passed; an appointment dated **today is still withdrawable**, since a same-day absence is the commonest reason a seat needs freeing. The rule is `Appointment::isAdminCancellable()`, read by both the view and the endpoint, and re-checked **under a row lock** inside the transaction so a duplicate submit or a race with kiosk check-in cannot slip past the page's unlocked read. Scope is enforced as in FR-ADM-06: the batch is fetched through the admin's managed college (foreign id → 404) and the appointment must belong to that batch (→ 404). | M |
| FR-ADM-08 | **College Admin Analytics (D-45).** A **college-scoped analytics page** (`GET /admin/analytics`) carrying the **same six cards as Director Analytics** — Vital-Sign Flags (FR-ANL-10), Visits per Month (FR-ANL-11), BMI Distribution (FR-ANL-12), Students Screened by Sex (FR-ANL-04) and Visits by Purpose — with **Clinic Visits by College replaced by Clinic Visits by Program**: one row per program the college offers, split Medical / Dental, sorted by total descending with an alphabetical tie-break, **zero-visit programs included** (the FR-ANL-09 rule one level down). Medical reads the capture-time `clinic_visits.course` snapshot (D-43); dental has no clinic_visits row and so attributes through the student's **current** program — the same stated limitation dental already carries for college. Visits whose snapshot is NULL (pre-D-43, never backfilled) or names a program the catalog no longer lists are collected in a single trailing **"Not specified"** row, so the card's headline can never disagree with the other cards. Filterable by **month** and by **program**; an unknown or foreign program degrades to "All programs" rather than erroring, exactly as the Director's college filter degrades (FR-ANL-13). **The college is derived from the admin's managed college and is never read from the request** — there is no `?college=` handling on this page at all, validated or otherwise (FR-AUTH-06 / FR-ADM-06), and the month picker likewise offers only months this college has data in. The FR-ANL-11 trend keeps ignoring the month (it is the whole-year view) but, unlike the Director's, **stays inside the college**: an admin is never shown another college's numbers, aggregated or not. No Flagged-Anomalies link — row-level vitals stay with the Nurse and Director (FR-ANL-05). | M |
| FR-ADM-09 | **Printable Monthly Clinic Report (D-46).** The College Admin shall be able to **print a month's report for their college** (`GET /admin/analytics/print`), rendered as **tables, not charts** — charts do not print reliably and the report has to be readable on paper. Contents, in order: a header carrying the university, the college's full name and code, the title, the month in full and a **generated-at timestamp with the name of the admin who generated it**; a summary line (total / medical / dental); **Clinic Visits by Program** (Program × Medical / Dental / Total, **zero-visit programs included**, with a totals row); **Visits by Purpose**; **Vital-Sign Flags** with counts and rates, carrying the same threshold captions the on-screen card uses (read from `config('healthpass.thresholds')`, never restated); **BMI Distribution** across the four buckets; **Students Screened by Sex** with counts and percentages; and a footer stating the report covers **data captured by HealthPass only**. Delivered as a **standalone Blade document plus `window.print()`** — the pattern the clearance form already uses (Module PRT) — loaded into a **hidden print frame on the analytics page and printed in place**, so the admin never leaves Analytics and the browser's print dialog *is* the preview; **not** a PDF package and **not** a CSV (FR-ANL-06 export stays struck by D-32). **Permanently light** (D-38): no `partials/theme-init`, no dark palette. The month and program filters **carry over from the analytics page's query string**, and a filtered report states its filter on the page. Scope is server-derived exactly as on FR-ADM-08: the college comes from the admin's managed college and **`?college=` is never read** (FR-AUTH-06 / FR-ADM-06). Every figure is built by the same `App\Services\ClinicAnalytics` under the same scope, so the printout cannot disagree with the screen it came from. | M |
| FR-ADM-10 | **College Activity Log (D-49).** A **read-only, college-scoped timeline** (`GET /admin/activity`) of everything that has happened to this college's batch requests: each **submission** (who submitted it, when, the student count and the reason) and each **Director decision** on it (**approved**, with the date appointments were generated for, or **rejected**, carrying the Director's written FR-DIRA-04 reason). Newest first, 15 per page. Its purpose is that a college **may have more than one College Admin** - nothing has ever constrained `users.managed_college_id` to a single account, and FR-AUTH-10 makes a second one easy to create - so two admins working the same college must be able to see each other's submissions, and the Director's decisions previously left no trace anywhere on the admin side. The page is equally useful with one admin and is **not conditional** on there being two. **DERIVED, NOT LOGGED:** there is **no activity-log table** and nothing writes an audit row; every entry is read back out of `batch_requests`, which already carries `requested_by`/`created_at` and `reviewed_by`/`reviewed_at`/`rejection_reason`, so the log **cannot drift from** the rows Batch Tracking renders. Scope follows FR-ADM-06 exactly: the college is `managedCollege()` and **`?college=` is never read**. **Deliberately excluded:** page views and other reads (this is a coordination log, not surveillance, and logging reads of a health system creates its own privacy problem), student self-bookings (the highest-volume event, which would bury the admin actions the page exists to show), and anything clinical - a College Admin must never see vitals or Fit/Unfit. | S |


**AC:** the CCS admin's search can never surface a CEA student; reason `others` without detail text is rejected; submitting 30 selected students creates exactly 30 pivot rows; batch status changes made by the Director appear in Tracking without admin action; withdrawing one student from a batch that had filled an hour to capacity makes that hour bookable again for a self-booking student, leaves every other student on the batch untouched, and is refused once that student has checked in at the kiosk.

### 4.5 Module DIR-A — Director Batch Approvals

| ID | Requirement | Priority |
|---|---|---|
| FR-DIRA-01 | The Director shall see all colleges' batch requests as rows (Batch ID + status badge, college, reason in quotes, student count, requested date — D-29, submitted date), with Approve / Reject buttons on pending rows only. | M |
| FR-DIRA-02 | **On Approve** the Director shall *confirm only* — the modal displays the admin's `requested_date` **read-only** and offers no date input (D-36, superseding D-29's adjust clause). The date used is read from the **locked** `batch_requests` row inside the transaction; a `scheduled_date` supplied by the client shall be ignored. The system then atomically (DB transaction): sets batch status `approved`, stamps `reviewed_by`/`reviewed_at` and `batch_requests.scheduled_date` **= `requested_date`**, creates **one appointment per listed student** (service = batch's service type, date = the requested date, `source = batch`, status `scheduled`, own `APT` reference), and writes each new `appointment_id` back to its `batch_request_students` row. **Two cases cannot be approved at all** — Approve is disabled on the row with a one-line notice *and* the endpoint refuses the POST, both directing the Director to reject and ask for a resubmission: (a) `requested_date` is NULL (pre-D-29), reason "predates the requested-date field — please resubmit"; (b) `requested_date` has **already passed** while the batch sat pending — confirm-only cannot move it and a cohort must never be scheduled into the past. A batch requested for **today** is still confirmable; only strictly past dates are stale. **(D-37)** The modal shall additionally display the batch's **clinic hour span read-only** (e.g. *7:00 AM – 10:00 AM (3 slots)*), and **two further** cases cannot be approved at all: a batch with **no hour span** (`requested_time` NULL — submitted before D-37), reason “predates the hourly-slot change — please resubmit”; and **(BR-23)** a batch requested for **today** **any** of whose span hours have already ended, reason “that time has passed — please resubmit for a later time”. Both disable Approve on the row with a one-line notice **and** are refused at the endpoint, which re-reads the rule off the locked row (`BatchRequest::elapsedSpanHours()`, the single definition shared by the page and the server). The fan-out shall write `scheduled_time` onto every generated appointment, distributing students across the span **12 per hour in pivot-row id order** (a deterministic assignment; the last block takes the remainder). | M |
| FR-DIRA-03 | Generated appointments shall immediately appear in each listed student's appointment list and dashboard, indistinguishable in downstream flow from self-booked ones. | M |
| FR-DIRA-04 | **On Reject** the Director shall supply a **written reason (required, 10–500 characters, server-validated by `RejectBatchRequest`)** — D-36. The modal's live character counter and disabled-until-valid submit are a client convenience only. The system then sets status `rejected`, stamps reviewer fields **and `batch_requests.rejection_reason`**, and creates no appointments. A replayed reject on an already-decided batch stays a no-op and shall **not** overwrite the reason already on record. | M |
| FR-DIRA-05 | Approved/rejected rows display static "✓ Approved" / "✕ Rejected" text; re-approval or re-rejection of a decided batch shall be impossible. | M |
| FR-DIRA-06 | ~~Batch approval shall bypass the per-day capacity check (a Director-approved cohort may exceed the self-booking capacity) — but the UI shall warn the Director when the selected date is already at/over capacity.~~ **AMENDED BY D-37 — WARN BECOMES HARD BLOCK.** If **any** one-hour slot in the batch's span (BR-22) has reached `hourly_capacity` **or has already ended (BR-23)** when the Director opens **or** submits the approval, approval shall be **refused**: the Approve button is disabled with the offending hour named, **and** the endpoint refuses the POST after re-reading the counts under `lockForUpdate()` (the UI is never the gate). The Director shall be directed to **reject with a reason** so the College Admin can resubmit for another date or start hour. **Consequence, stated plainly:** combined with D-36's confirm-only approval, a batch whose slots filled up between submission and review has **exactly one outcome — rejection and resubmission**. That is the intended workflow, not a bug — the alternatives are approving a cohort into a clinic that cannot process it, or letting the Director silently move the date, which D-36 removed on audit-trail grounds. | M |

**AC:** approving a 25-student batch creates exactly 25 appointments inside one transaction (kill the process mid-way in a test → zero partial state); rejected batches generate nothing; double-clicking Approve does not duplicate appointments; a `scheduled_date` injected into the approve POST is ignored and the cohort lands on `requested_date`; a NULL-`requested_date` batch and one whose requested date has passed both cannot be approved (a batch requested for today still can); a reject with no reason, a <10-character reason, or a >500-character reason is refused and leaves the batch pending.

### 4.6 Module KSK — Kiosk (1080×1920 portrait, touch-first)

The kiosk is the route `/kiosk` rendered full-screen in Chromium kiosk mode on the Raspberry Pi at `http://localhost` (Web Serial secure-context requirement, §11.3). The `#F6F2ED` panel fills the viewport with `--k-zoom` scaling (D-18); the hardware target is a **15.6″ 1080×1920 portrait** touchscreen (D-26 — supersedes the original 7″ 800×480 landscape display and its letterbox/design-canvas references).

| ID | Requirement | Priority |
|---|---|---|
| FR-KSK-01 | **Welcome screen:** pulsing QR target + "Tap to Scan Your ID" on the left, divider, "Lost ID? Log in with email" on the right. The page shall keep an invisible focused input so a USB QR scanner (keyboard-wedge) can type the `qr_token` + Enter at any time. | M |
| FR-KSK-02 | **Email login screen:** email + password fields with an on-screen QWERTY virtual keyboard (4 rows incl. digits and `@ . _ -`, Delete/Space/Enter; Enter orange, Delete peach); typing routes to the focused field; password has an eye toggle; "← Cancel" returns to Welcome. | M |
| FR-KSK-03 | **Identity Confirm:** large initials avatar, "Identity Verified ✓", greeting with first name, college/course/year/student number, "That's me — Continue" and "Not you?" (resets to Welcome). | M |
| FR-KSK-03a | **Walk-in check (after Identity Confirm, before Privacy Consent):** the kiosk shall look up whether the identified student has **any non-cancelled appointment dated today — medical *or* dental**. **If none exists** (literally nothing booked today), a "No Scheduled Clearance Today" screen shall explain that no appointment is booked for today and offer **"Proceed as Walk-in"** (→ Privacy Consent) and an exit/cancel action (→ Welcome). **If a same-day appointment exists** (medical or dental), this screen is skipped and the flow proceeds to Privacy Consent. This screen is a UI gate only. The submit-time linkage in FR-KSK-12 / BR-10 remains **MEDICAL-only and unchanged** (`appointment_id` = today's medical appointment else NULL): a dental-only same-day booking suppresses the notice but still proceeds through the medical vitals flow and is recorded as a **walk-in** (`appointment_id` NULL) — dental stays scheduling-only (Decision D-3) and never links. | S |
| FR-KSK-04 | **Privacy Consent (per session):** RA 10173 text with "I Agree — Proceed" and "Decline" (Decline resets to Welcome and stores nothing). Agreement timestamps `clinic_visits.privacy_consent_at`. | M |
| FR-KSK-05 | **Vitals sequence** of four steps with progress dots — Height (cm) → Weight (kg, with computed BMI panel showing value + status + "from X cm + Y kg") → Temperature (°C) → Blood Pressure (systolic/diastolic mmHg + Heart Rate bpm in a peach sub-panel). Each step has a 3-phase UI: ready (instructions) → scanning (animation) → captured (large value + unit + status badge), with Retry and Next actions. | M |
| FR-KSK-06 | **Manual entry shall exist on every vital step as a first-class path** (Decision D-7): a "Enter manually" action opens a numeric on-screen pad; manual values pass the same validation ranges. The visit's `vital_signs.entry_method` records `sensor`, `manual`, or `mixed`. | M |
| FR-KSK-07 | **Sensor capture** (progressive enhancement over FR-KSK-06): the page connects to the microcontroller via the **Web Serial API**, reads the combined reading line (format §11.2), parses it, and fills the current step's value. Sensor unavailability (no port, parse failure, timeout) shall degrade gracefully to manual entry with a non-blocking notice — never a dead end. | M |
| FR-KSK-08 | Client- and server-side plausibility validation of vitals: height 50–250 cm; weight 10–300 kg; temperature 30.0–45.0 °C; systolic 60–260; diastolic 30–160; heart rate 30–220 bpm. Out-of-range input prompts re-entry. | M |
| FR-KSK-09 | BMI shall be computed (not entered) as weight(kg) ÷ height(m)², rounded to 1 decimal. | M |
| FR-KSK-10 | **Questionnaire:** a 3-column grid of 9 system cards — Vision/Eyes, Hearing/Ears, Nose & Throat, Skin, Respiratory/Breathing, Heart/Circulation, Digestive/Stomach, Bones & Joints, Nervous/Neurological — each answered Yes/No; plus a full-width pregnancy question (Yes reveals an inline month calendar for Last Menstrual Period; future dates disabled). Footer shows "{N} of 10 answered"; Review & Submit is disabled until all 10 are answered. | M |
| FR-KSK-11 | **Review screen:** two cards (Vital Signs with flagged items in orange + ⚑; Questionnaire as Yes/No badges) and "Submit to Clinic →". | M |
| FR-KSK-12 | **Submit** shall create, in one transaction: a `clinic_visits` row (status `captured`, reference `HP-YYYY-####`, `login_method`, consent + check-in timestamps, `appointment_id` = today's booked appointment for that student if one exists else NULL = walk-in, **plus the capture-time `college_id` and `course` snapshots read in one query from the server-side bound student — D-17 and D-43; a missing college fails loudly, a missing program stores NULL**) + its 1:1 `vital_signs` row (with flag booleans computed per §7.4) + its 1:1 `screening_responses` row. | M |
| FR-KSK-13 | **Complete screen:** success check, "Submitted! … proceed to the nurse's station", and a countdown pill auto-resetting the kiosk to Welcome after **12 seconds** (or instantly via a Done tap). All session state is cleared on reset. | M |
| FR-KSK-14 | The kiosk shall never display Fit/Unfit, case categories, or any clinical interpretation beyond the per-vital status badge (e.g., "Slightly Elevated"). | M |
| FR-KSK-15 | An idle timeout (no interaction for 90 s mid-flow) shall discard the session and reset to Welcome, to protect privacy on an abandoned kiosk. | S |
| FR-KSK-16 | A discreet staff exit (e.g., 5 taps on the logo corner + nurse password) shall close kiosk mode; students cannot navigate out of `/kiosk` otherwise. *(Implemented per D-19 as full email + password nurse authentication, landing in the authenticated nurse queue — not a bare password prompt.)* | S |

**AC:** scanning a valid QR (including a multi-line physical-ID payload — the IDNo is extracted before the lookup) lands on Identity in < 2 s; an invalid or unrecognized token shows a brief inline error and refocuses the hidden input; declining consent stores zero rows; pulling the MCU's USB cable mid-flow still allows finishing via manual entry; a submitted visit with no appointment shows as walk-in in the queue; after the 12 s countdown the next student sees a clean Welcome with no residue of the previous session.

### 4.7 Module NRS — Nurse Live Queue & Encoding

| ID | Requirement | Priority |
|---|---|---|
| FR-NRS-01 | **Live Queue** shall list visits with status `captured`, **oldest first (first come, first served)** — the top row is the longest-waiting student, tagged "NEXT" with a peach highlight; new submissions append at the bottom. Columns: Student (initials avatar + name), College, Vitals Summary inline (flagged values bold orange), Flags column (badges for temp/bp/bmi or "—"), capture Time, and an "Encode Result" action (primary button on the top row). | M |
| FR-NRS-02 | The queue shall refresh by **polling** (JS `fetch` every 3–5 s) updating rows in place; the header shows a blinking LIVE pill and "{n} students waiting · updated just now". | M |
| FR-NRS-03 | **Encode Result ("Doctor's Assessment"):** shall display the visit's full vitals (with flags), all questionnaire answers, and student identity; the nurse sets Result (**Fit/Unfit — required**), Medical Case Categories (**optional, multi-select — zero or more** of Alimentary System, Respiratory System, Musculo-Skeletal System, Integumentary System, Urinary System, Metabolic Endocrine System, Cardiovascular System, Eyes, Ears, Nose & Throat Disorders; a case can span several systems — D-23; the kiosk's vision/hearing answers are decision support for the EENT pick), Purpose (**shown only when the visit's appointment carries NO student-supplied purpose** — walk-ins, batch-booked, or pre-D-28 appointments; in that case optional, one of Off Campus Procedure, On-the-job Training, Field Trip/Educational Tour, Sports Activities, or **Others** — picking Others reveals a required free-text "Specify" field for the exact event, max 120 chars, stored in `purpose_other` — D-24. When the student **chose the purpose at booking (D-28)** the input is hidden, the student's choice is shown read-only, and it is copied onto the clearance record on Save so the printed form auto-populates), the **Physical Signs Disorder of exam findings** (nine YES/NO rows — SKIN, ABDOMEN (GIT), HEENT, GUT, CHEST/LUNGS, EXTREMITIES, HEART/CVS, NEUROLOGICAL, BREAST — each optional; the physician examines the student, the nurse records the findings; placed before Nurse Notes; a kiosk questionnaire answer pre-checks its matching row — **YES and NO alike** — for the nurse to confirm or correct; rows without a kiosk counterpart (GUT, BREAST) open unanswered — D-22 as amended by D-25), and free-text Nurse Notes. | M |
| FR-NRS-04 | **Save & Close** shall create the 1:1 `clearance_records` row (with `encoded_by`, `encoded_at`, pre-filled physician fields per §7.5) and flip the visit to `encoded`, removing it from the Live Queue. Encoding shall be idempotent — a visit can be encoded exactly once; subsequent opens are read/reprint-only. | M |
| FR-NRS-05 | **Preview & Print** shall render the official clearance form (Module PRT) in an iframe inside the encode screen and trigger `window.print()` on the iframe's content window; printing stamps `printed_at`. Reprint is allowed and re-stamps `printed_at`. | M |
| FR-NRS-06 | The nurse navigation shall include **Enable Kiosk Mode** — a nurse-only page (D-27) to **enroll the current browser/terminal as a trusted kiosk DEVICE**, list enrolled devices, and revoke each. Enrolling generates a long random token (only its **hash** is stored, table `kiosk_devices`), sets it in a long-lived HttpOnly cookie on that browser, and shows a one-time `?device_token=…` provisioning URL for the Pi launcher. `KioskAccess` then admits any request bearing a valid, un-revoked device token — so the terminal opens the kiosk without anyone signing in at the screen, while the DEVICE (never client-supplied identity) is what is trusted. | M |
| FR-NRS-07 | If a captured visit's linked appointment exists, encoding shall also mark that appointment `completed`. | S |
| FR-NRS-08 | Captured visits shall persist indefinitely until encoded — a backlog at day's end is preserved across restarts (ties to SM-4). | M |
| FR-NRS-09 | **Nurse Dashboard** (D-44) — the nurse's landing page shall show four stat tiles (encoded today, encoded this month, awaiting encode, flagged vitals this month) and a **clinic-wide** encode history of all clearance records, newest first, filterable by month and result and searchable by student name or reference number, paginated, showing the **encoding nurse**. Rows link to the existing read-only encode view and reprint action. | M |

**AC:** two browser windows on the queue both show a new kiosk submission within one poll cycle; saving without a Result is blocked with a field error; after Save & Close the row disappears from both windows; the printed page matches the official form (SM-3); killing the server with 5 un-encoded visits and restarting shows all 5 still queued.

### 4.8 Module PRT — Clearance Printing

| ID | Requirement | Priority |
|---|---|---|
| FR-PRT-01 | The print view shall be a dedicated Blade template reproducing form **DHVSU-QSP-OSS-004-FO002-R03 — MEDICAL CLEARANCE**, issuing office "Office of Student Welfare and Formation — Health Services Unit", matching the official layout field-for-field. | M |
| FR-PRT-02 | The form shall populate: student identity fields (name, course/year, age, sex, civil status, address, birth details — **no student number and no college**: the official form has neither field, D-22/D-25), vitals (height, weight, BMI, temperature, BP, heart rate), the **Physical Signs Disorder YES/NO bubbles from the nurse-encoded physician exam findings** (`clearance_records.ps_*`; an unanswered row prints blank), the **pregnancy question + LMP from the kiosk questionnaire**, the encoded Result, Purpose where set (an **Others** purpose shades the form's "Others, Specify:" bubble and prints the specified event on its line, clipped to fit — D-24), encode date, and nurse notes under REMARKS. **Case Category is not printed** — the physician hand-writes case details under REMARKS, judging the shaded YES physical signs (Decision D-22). | M |
| FR-PRT-03 | **Respiratory Rate shall be intentionally left blank** — it is not a captured vital (Decision D-6). | M |
| FR-PRT-04 | The physician block shall be pre-printed: **REYNALDO S. ALIPIO, MD — University Physician — License No. 60252** (stored per record in `clearance_records.physician_name` / `physician_license_no` defaults), with a blank signature line for wet signing. | M |
| FR-PRT-05 | Print styling shall use a print CSS that fits one page (A4/Letter as the clinic stocks), suppressing app chrome. Implementation is HTML + `window.print()`; dompdf (`barryvdh/laravel-dompdf`) is a Could-have upgrade if a saved PDF artifact is later required. | M / C |

**AC:** physical print compared side-by-side with a blank official form by clinic staff — all labels, ordering, and the physician block match; respiratory rate is blank; long nurse notes do not break the one-page layout.

### 4.9 Module ANL — Director Dashboard, Analytics & Flags

| ID | Requirement | Priority |
|---|---|---|
| FR-ANL-01 | **Director Dashboard** shall show KPI cards plus two preview panels — Pending Batch Approvals (count + preview rows → Batch Approvals) and Flagged Anomalies (count + preview rows → Flagged Anomalies) — each with "View all →". | M |
| FR-ANL-02 | ~~**Analytics — Medical Cases by College:** a horizontal stacked bar chart (Chart.js), one row per college/unit (all 12 units), each bar segmented by the 8 medical-system categories, sorted by total case volume descending, with a total-cases headline (e.g. '235 total cases'). Subtitle: 'Total cases per college, broken down by medical system — sorted by volume.' Students only — Faculty and NASA excluded. Series colors follow the prototype legend. Source: `clearance_case_categories` joined through encoded `clearance_records` (one count per record × category — a multi-category case counts once in each of its systems, D-23), grouped by `clinic_visits.college_id` (the capture-time college snapshot — a later transfer does not re-attribute past cases, FR-STU-09) × `case_category`.~~ **— Superseded (D-32):** the system never sees clinic-wide caseload, so this chart is indefensible; replaced by FR-ANL-09 (Clinic Visits by College). | ~~M~~ |
| FR-ANL-03 | ~~**Summary of Medical Cases matrix:** 8 medical-system rows × 12 college-code columns + a TOTAL column and a totals row. Fixed column order: COE, CEA, CBS, CAS, CSSP, CCS, CHTM, CIT, LAW, GS, SHS, LHS. Subtitle: 'Rows = medical system · Columns = college · Faculty & NASA excluded.' Source: `clearance_case_categories` joined through encoded `clearance_records` (one count per record × category, D-23), grouped by `clinic_visits.college_id` (capture-time college snapshot, FR-STU-09) × `case_category`; records with no categories contribute nothing to the cells — their count appears in a card-level footnote ('N encoded without category'). **Placement (D-30, v1.9):** the matrix is not a separate card — it renders inside the FR-ANL-02 chart's 'View as table' toggle, since both present the same college × category dataset in different formats.~~ **— Superseded (D-32):** case-category matrix removed with the case-category concept; no replacement. | ~~M~~ |
| FR-ANL-04 | **Students Screened by Sex** (Chart.js donut, retitled from "By-Sex donut", D-32): Male vs Female counts of **students screened** (captured kiosk visits) in scope, center total, legend with count + %. Counts people screened — so it already fits the system-collected scope — and now **obeys the college filter** as well as the month filter (FR-ANL-13). | M |
| FR-ANL-05 | **Flagged Anomalies screen:** three stat cards (High Blood Pressure, Fever, Abnormal BMI counts) and a table (Student, College, Flag badge, Value, View) — **the Category column is dropped (D-32)** — where the College column shows the capture-time college snapshot (`clinic_visits.college_id`, FR-STU-09) — not the student's current college, so a later transfer does not re-label a past flag — sourced from visits where any of `is_bp_flagged / is_temp_flagged / is_bmi_flagged` is true. | M |
| FR-ANL-06 | ~~**Export:** an Export action on Analytics and Flagged Anomalies producing at minimum a CSV of the underlying rows (print-friendly view acceptable as fallback).~~ **— Superseded (D-32):** analytics is on-screen only — no monthly "Summary of Medical Cases" print, no analytics CSV, no Flagged-Anomalies CSV. | ~~S~~ |
| FR-ANL-07 | All analytics shall compute from **captured** data (D-32) — case statistics are removed. Medical visits count at kiosk **check-in** and vital flags count from **capture** (`clinic_visits` + `vital_signs`); dental visits count from **completed** dental appointments (D-33). The old "encoded records only" rule is retired with the case statistics. | M |
| FR-ANL-08 | ~~**Cases by Medical System:** a horizontal bar chart (Chart.js), one bar per medical-system category (the 8 above), showing the overall total per system across all units, sorted descending, the per-system total drawn at the bar's end. **Each bar splits into a Male and a Female segment (D-31):** Male in the system's full-strength FR-ANL-02 series color, Female in a 50%-white tint of it; the stacked bar's full length remains the system total. Same source/scope as the matrix (FR-ANL-03) plus the student's `student_profiles.sex` for the split.~~ **— Superseded (D-32):** case-category charts removed; no replacement — the system holds no clinic-wide case data. | ~~M~~ |
| FR-ANL-09 | **Clinic Visits by College:** a horizontal stacked bar chart, one row per college (all 11 since D-43, **zero-visit rows included**), each bar segmented Medical `#FF8C2A` / Dental `#2563EB`, sorted by total visits descending, with a total-visits headline. **Medical** = kiosk check-ins (`clinic_visits`, grouped by the capture-time `college_id` snapshot, FR-STU-09); **Dental** = completed dental appointments (D-33), grouped by the student's **current** college — no capture-time snapshot exists for dental (**stated limitation**). A "View as table" toggle shows college × service type. **Amended (D-46): when the FR-ANL-13 college filter selects a SINGLE college, this card is replaced by a by-Program chart for that college** — one college bar has nothing to compare itself against, and the programs are the same visits one level down (the FR-ADM-08 rows, from the same `ClinicAnalytics::visitsByProgram()` builder, zero-visit programs included). With "All colleges" selected the card is unchanged. The card also carries a **Visits by Purpose** mini bar chart, sourced from the linked appointment's `purpose`/`purpose_other`; a visit with no linked appointment or no purpose falls into a "Walk-in / not specified" bucket. Obeys month + college filters (FR-ANL-13). | M |
| FR-ANL-10 | **Vital-Sign Flags card:** three stat tiles — High BP, Fever, Abnormal BMI — each showing the **count** and the **rate** (% of the month's captured screenings). Recomputed server-side from `vital_signs` flags; obeys month + college filters. Row-level detail remains on the Flagged Anomalies screen (FR-ANL-05). | M |
| FR-ANL-11 | **Visits per Month trend:** a line chart across all months that have data, with **two series** — medical screenings (captured kiosk visits) and completed dental appointments (D-33). It **ignores the page filters by design** — always the whole-year, all-college view — to show the year's trajectory regardless of the selected month/college. | M |
| FR-ANL-12 | **BMI Distribution:** four rule-based buckets of captured screenings — Underweight (< 18.5), Normal (18.5–24.9), Overweight (25–29.9), Obese (≥ 30). Descriptive only, **no profiling** (the no-AI lock D-1 stands). Obeys month + college filters. | C |
| FR-ANL-13 | **Page filters:** the existing month picker plus a new **college dropdown** (default "All colleges"). Both scope every card on the page **except the FR-ANL-11 trend**, which is whole-year / all-college by design. **Amended (D-46): selecting a single college also swaps the FR-ANL-09 by-College chart for a by-Program chart of that college**; "All colleges" restores it. | M |

> **Where these live (D-45):** the card builders behind FR-ANL-09..13 (and the amended FR-ANL-04) were extracted out of `Director\AnalyticsController` into **`App\Services\ClinicAnalytics`** and are now **shared by the Director page and the College Admin page (FR-ADM-08)**. The service is constructed with a fixed scope — month, optional college, optional program — so a caller cannot ask one card for a different college than another. Behaviour of every card is unchanged; the Director's acceptance test passed unedited across the refactor.

**AC:** seeding N medical + M dental visits in a college yields exactly those two bar segments and the matching totals in Clinic Visits by College (FR-ANL-09); a freshly captured flagged visit counts in the Vital-Sign Flags tiles immediately (FR-ANL-10), its rate recomputed against the month's screening count; selecting a college in the dropdown re-scopes every card except the Visits-per-Month trend (FR-ANL-11/13); and the superseded Medical Cases chart, Summary matrix, Cases-by-System bars, and every Export/print action are absent from the page (D-32).

### 4.10 Module HW — Kiosk Hardware Integration

| ID | Requirement | Priority |
|---|---|---|
| FR-HW-01 | The sensor rig shall be wired to a single microcontroller (Arduino or ESP32) which streams a **combined reading line over USB serial** in the agreed format (§11.2) at a fixed cadence and/or on request. | M |
| FR-HW-02 | The kiosk page shall read the MCU via the **Web Serial API**, available only in a secure context — satisfied by serving the app at `http://localhost` on the Pi (Decision D-9). | M |
| FR-HW-03 | Sensors: ultrasonic distance (height), HX711 load cell (weight), MLX90614 IR thermometer (temperature), and a digital BP monitor **with verified serial output** (also yields heart rate). | M |
| FR-HW-04 | The **Week-1 spike** shall validate, before any bulk hardware purchase: (a) one sensor reading reaching a Chromium page on the Pi via Web Serial, and (b) the candidate BP monitor actually exposing readable serial output. | M |
| FR-HW-05 | The kiosk shall handle serial disconnect/reconnect (cable jiggle, MCU reset) without page reload where possible, and document Chromium kiosk-mode permission persistence behaviour for unattended operation. | S |
| FR-HW-06 | A physical enclosure shall mount the Pi, screen, MCU, sensors, and QR scanner with managed cabling and a single power feed. | S |
| FR-HW-07 | Defense-day kit: spare MCU, spare cables, and the documented manual-entry fallback drill. | S |

**AC:** with the full rig on the Pi in kiosk mode, a student completes all four vitals from live sensors; yanking the BP monitor mid-step degrades to manual entry (FR-KSK-07) and the session still submits.

---
## 5. Business Rules (normative)

### 5.1 Scheduling
- **BR-01** Clinic hours: 7:00 AM–5:00 PM, daily (incl. weekends), Campus Clinic, Main Building. Past dates are never bookable. **(D-37)** Those hours are divided into **ten bookable one-hour slots** — 7–8 AM through 4–5 PM, **the lunch hour 12–1 PM included** — derived at runtime from `healthpass.clinic_hours` by `App\Services\ClinicScheduleService`; the slot grid is never hardcoded in a view. Every appointment created from D-37 onwards carries one of those slots in `appointments.scheduled_time`.
- **BR-02** Each clinic day has one global capacity (config value, Decision D-4) — **`healthpass.daily_capacity`, raised from 40 to 120 by D-37**. A day at capacity displays "FULL" and rejects self-bookings server-side. Since D-37 a day is **also** FULL when every one of its ten slots is at the per-hour cap (BR-21); the daily cap remains enforced separately because it is the only counter that sees pre-D-37 appointments, which sit in no slot.
- **BR-21** *(D-37)* **Per-hour capacity.** Each one-hour slot holds `healthpass.hourly_capacity` = **12** appointments — a kiosk session is assumed to take at most 5 minutes, so 12 students fit in an hour, and 12 × 10 hours = the 120 daily cap. **Medical and dental share ONE counter:** a dental appointment consumes an hour-slot exactly like a medical one, because the constraint being modelled is **clinic congestion**, not kiosk throughput alone. Cancelled appointments free their seat. Enforced server-side in `StoreAppointmentRequest` (unlocked read) **and again under `lockForUpdate()` inside the booking transaction** — the Form Request's read races with a concurrent booking, so the locked re-check is the authoritative gate. The error names the hour ("The 9:00 AM – 10:00 AM slot is fully booked"), never a bare "full".
- **BR-23** *(D-37)* **Elapsed hours.** On **today**, a one-hour slot stops being bookable once it has **ended** — at 12:00 sharp the 11:00 AM–12:00 PM slot is gone and 12:00–1:00 PM is the earliest still available. The boundary is the slot's END, not its start, which keeps it consistent with BR-20's existing "today is bookable right up to 16:59" behaviour. Applies to **all three** write paths: student self-booking (the slot is greyed as "Past" and refused server-side), the College Admin's batch **start hour** (only the start needs checking at submission — a span runs forwards, so if its first hour is still open none of the later ones can have elapsed), and **Director approval**, where **any** elapsed hour in the batch's span refuses the approval. The Director case exists because D-36 still permits approving a batch requested for *today*: the hours can simply end between submission and review, and fanning out then would create appointments for a time that has been and gone. A partially-elapsed span is refused too — some of the cohort would be scheduled into the past. Only today is affected: a future date has no elapsed hours. Evaluated **server-side against the Asia/Manila clock and never the browser's** — the student calendar and the batch form receive the elapsed list from the server, exactly as BR-20's cutoff does. A date whose every hour is either full or elapsed is greyed out in the calendar (BR-02), which is what makes BR-20's after-closing case fall out naturally. Unlike the capacity checks this needs **no locked re-check**: no concurrent request can change whether an hour has ended, so there is nothing to race.
- **BR-22** *(D-37)* **Batch span.** A batch request occupies `ceil(students ÷ hourly_capacity)` **contiguous** one-hour slots starting at the College Admin's chosen hour (`batch_requests.requested_time`, span length in `requested_blocks`). The span length is always computed server-side from the roster — a client-supplied block count is ignored. A submission is rejected when (a) the roster exceeds 120 students, since no start hour fits it in one day; (b) the span would run past 5:00 PM; or (c) **any** hour in the span is already at the per-hour cap — the error names the offending hour. Checked in the Form Request and re-checked under lock at write time.
- **BR-03** Service types: `medical` (full clearance loop) and `dental` (scheduling only, Decision D-3).
- **BR-04** One active (non-cancelled) appointment per student per service per date — enforced by validation, **not** a DB unique constraint (cancelled rows would collide). **D-37 deliberately left this per DATE, not per slot:** a student with a 7 AM medical appointment still cannot book a second medical one at 2 PM the same day.
- **BR-20** *(pending adviser sign-off, Decision D-21)* Same-day closing cutoff: once the local clock (Asia/Manila) reaches the clinic closing hour — config key `healthpass.closing_hour`, default **17** (5:00 PM), aligned with BR-01 — **today** can no longer be self-booked; the student is directed to book for the next day onwards. Enforced server-side in `StoreAppointmentRequest` (authoritative) with the message "The clinic is closed for today. Please book for the next day onwards."; the availability endpoint additionally marks today unavailable after the cutoff so the calendar greys it out (cutoff computed server-side, never from the browser clock). Future dates are unaffected.

### 5.2 Batch requests
- **BR-05** A College Admin requests only for their own college (server-enforced via `managed_college_id`).
- **BR-06** Reason is required; `others` additionally requires `reason_detail`. Valid reasons: graduation clearance, OJT/practicum, general enrollment, scholarship, sports/athletics, field trip/educational tour, others.
- **BR-07** ≥ 1 student per batch.
- **BR-08** Director approval **auto-generates one appointment per listed student** on the Director-selected date, transactionally, and links each via `batch_request_students.appointment_id`.
- **BR-09** Rejection generates no appointments and is terminal; decided batches are immutable.

### 5.3 Visits & linkage
- **BR-10** A kiosk submission links to the student's appointment **for that date** if one exists; otherwise it is a walk-in (`appointment_id` NULL). Walk-ins are first-class — they flow through queue → encode → print identically.
- **BR-11** A clinic visit is `captured` until the nurse saves an encode, after which it is `encoded` forever.
- **BR-12** Captured visits are never auto-deleted or expired (reliability, SM-4).

### 5.4 Rule-based vital flags — screening signals, not diagnoses

| Vital | Normal reference | Flag condition (locked) |
|---|---|---|
| Temperature | 36.1–37.2 °C | **> 37.2 °C** → `is_temp_flagged` ("Fever") |
| Blood pressure | < 120/80 mmHg | **Systolic ≥ 140 OR Diastolic ≥ 90** → `is_bp_flagged` ("High Blood Pressure") |
| BMI | 18.5–24.9 | **≥ 30.0** → `is_bmi_flagged` ("Abnormal BMI / Obese") |

- **BR-13** Thresholds live in **one place** — `config/healthpass.php` — consumed by the kiosk badges, queue flags, and Director anomaly screen alike. (This resolves the earlier 130/85-vs-140/90 discrepancy between planning documents: **140/90 is canonical**, Decision D-10.)
- **BR-14** Flags are computed at capture time and stored as booleans (queryable), and are advisory only — they never block submission or pre-determine Fit/Unfit.

### 5.5 Encoding & clearance
- **BR-15** Only the Nurse encodes; the screen is titled "Doctor's Assessment" but no Doctor login exists (Decision D-2).
- **BR-16** `result` (Fit/Unfit) is required to save; case category and purpose are optional. An **Others** purpose additionally requires its specify-event text (D-24). Purpose is **nurse-entered on the encode screen only when the visit's appointment carries no student-supplied purpose** (walk-in, batch, or pre-D-28 booking); when the student chose a purpose at booking, encode hides its purpose input and copies the student's choice onto the clearance record on Save (D-28).
- **BR-17** The printed form carries the pre-printed physician identity (REYNALDO S. ALIPIO, MD, License No. 60252) and a blank line for wet signature; respiratory rate is intentionally blank.
- **BR-18** Students see results only in My Records after encoding — never on the kiosk.

### 5.6 Reference numbers
- **BR-19** `APT-YYYY-####` (appointments), `BR-YYYY-###` (batch requests), `HP-YYYY-####` (clinic visits / clearances). Sequences are per-year, zero-padded, generated server-side, unique-indexed.

---

## 6. Data Requirements

### 6.1 Entity overview (10 domain tables)

The finalized ERD (rendered June 10, 2026 in the project workspace) is the schema of record. It is the `HealthPass_Context.md` §8 schema plus **exactly two locked deltas**:

1. `batch_requests.scheduled_date` `DATE NULL` — the FINAL appointment date stamped at approval (Decision D-5; since D-29 the date originates from the admin's `requested_date` rather than being invented by the Director, and since **D-36** approval simply copies it — the Director confirms, never adjusts).
2. `vital_signs.entry_method` `ENUM('sensor','manual','mixed') DEFAULT 'sensor'` — provenance of the captured vitals (Decision D-7).

### 6.2 Data dictionary (condensed)

| Table | Purpose | Key columns (PK bold, FK →) |
|---|---|---|
| `colleges` | Reference: the 11 colleges (12 before D-43 removed Senior High School) | **id**, code UQ, name |
| `users` | All accounts, 4 roles | **id**, role, name, email UQ, email_verified_at, password, managed_college_id →colleges (admins only), status, must_change_password (BOOLEAN default false — D-35), **last_active_at NULL (new — TIMESTAMP, D-48)** (when the account last made a request that was actually served; written by `RecordLastActive` at most once per account per minute, and via the query builder so `updated_at` is untouched. **NULL on accounts not seen since the column existed — deliberately NOT backfilled**, and rendered "Never" on the Director's Staff Accounts screen) |
| `student_profiles` | 1:1 student detail | **id**, user_id UQ →users, college_id →colleges, student_number UQ, first/middle (opt.)/last name, sex, course, year_level, date_of_birth, place_of_birth, civil_status, address, qr_token UQ (IDNo parsed from physical ID QR = student_number; provisional until linked), privacy_consent_at |
| `appointments` | Solo + batch-generated bookings | **id**, reference_no UQ, student_id →users, service_type, purpose NULL (varchar(50) — student-chosen clearance purpose at self-booking: the four locked values or 'Others', validation-gated; NULL for dental / batch / pre-D-28 — D-28), purpose_other NULL (varchar(120) — the Others specify-event text, D-28), scheduled_date, **scheduled_time NULL (new — TIME, D-37)** (the one-hour clinic slot, canonical `'H:i:s'`; required on every booking made from D-37 onwards, **NULL on pre-D-37 rows which were deliberately NOT backfilled** — they belong to no slot, render as "—", and are counted only by the daily cap), status (scheduled/checked_in/completed/cancelled), source (self/batch), batch_request_id →batch_requests NULL, created_by →users NULL — index(`scheduled_date`,`scheduled_time`,`status`) **(new, D-37)** makes the per-slot count cheap and serves the daily count as a leftmost prefix |
| `batch_requests` | College Admin cohort requests | **id**, reference_no UQ, college_id →colleges, requested_by →users, reason, reason_detail, service_type, requested_date NULL (admin-proposed clinic date, set at submission; NULL only pre-D-29 — D-29), **requested_time NULL (new — TIME, D-37)** (admin-chosen START hour of the batch's span, canonical `'H:i:s'`), **requested_blocks NULL (new — TINYINT UNSIGNED, D-37)** (span LENGTH in whole hours = `ceil(students ÷ 12)`, always computed server-side. **A block count, not an end time, and never both:** every consumer wants the length in slot units — the validator loops it, the fan-out loops it, the UI prints "(3 slots)" — an end time would be re-divided at each of those sites and is ambiguous about whether it names the start or the end of the last hour; and the count is not safely re-derivable later, since a student leaving the roster would silently shrink an already-approved span. Both columns NULL only on pre-D-37 batches, which — like a NULL `requested_date` under D-36 — **cannot be approved at all**), **scheduled_date NULL (new)** (final date, stamped at approval; **= `requested_date` since D-36** — D-5/D-29/D-36), status (pending/approved/rejected), **rejection_reason NULL (new — TEXT, D-36)** (the Director's written reason, 10–500 chars enforced in `RejectBatchRequest`; NULL on pending/approved rows and on rejections made before D-36), reviewed_by →users NULL, reviewed_at |
| `batch_request_students` | Batch ↔ student pivot | **id**, batch_request_id →, student_id →users, appointment_id →appointments NULL (set on approval); UQ(batch_request_id, student_id) |
| `clinic_visits` | One kiosk session | **id**, reference_no UQ, student_id →users, **college_id →colleges (new — capture-time snapshot for transfer-proof analytics, FR-STU-09)**, **course NULL (new — VARCHAR(120), D-43)** (capture-time snapshot of the student's academic **program**, frozen by `SubmitKioskVisit` from the server-side bound student, mirroring the `college_id` snapshot of D-17; `student_profiles.course` stays live, so without this a program shift would silently restate every past per-program report. **Nullable and NEVER backfilled** — a visit captured before D-43 has no honest answer and renders as "—", exactly the pattern `appointments.scheduled_time` uses for pre-D-37 rows. A profile with no program stores NULL rather than failing: unlike a missing college, a missing program cannot mis-attribute the visit), appointment_id →appointments NULL (=walk-in), login_method (qr/email), status (captured/encoded), privacy_consent_at, checked_in_at |
| `vital_signs` | 1:1 vitals per visit | **id**, clinic_visit_id UQ →, height_cm, weight_kg, bmi, temperature_c, heart_rate_bpm, bp_systolic, bp_diastolic, is_temp_flagged, is_bp_flagged, is_bmi_flagged, **entry_method (new)** |
| `screening_responses` | 1:1 questionnaire per visit | **id**, clinic_visit_id UQ →, vision, hearing, nose, skin, respiratory, heart, digestive, bones, nervous (booleans), is_pregnant, last_menstrual_period NULL |
| `clearance_records` | 1:0..1 encoded result per visit | **id**, clinic_visit_id UQ →, encoded_by →users, result (Fit/Unfit), purpose NULL (varchar(50) — the four locked values or 'Others', validation-gated, D-24), purpose_other NULL (varchar(120) — the Others specify-event text, D-24), nurse_notes, ps_skin…ps_breast (9× boolean NULL — physical-signs exam findings, D-22), physician_name (default Alipio), physician_license_no (default 60252), encoded_at, printed_at |
| `kiosk_devices` | Trusted kiosk terminals (**table #11 — D-27**, added July 12 2026; kiosk device-auth for FR-NRS-06; was #12 before `clearance_case_categories` was removed by D-32) | **id**, name, token_hash UQ (sha256 of the device token — **plaintext never stored**), created_by →users NULL (enrolling nurse, nullOnDelete), revoked_at NULL (set = revoked), timestamps |

Framework tables created by Laravel/Breeze (`password_reset_tokens`, `sessions`, `cache`, `jobs`) exist but are **not** counted among the 10 domain tables.

### 6.3 Migration order (FK-safe)

`colleges` → `users` → `student_profiles` → `batch_requests` → `appointments` → `batch_request_students` → `clinic_visits` → `vital_signs` → `screening_responses` → `clearance_records`

### 6.4 Indexes (beyond PK/UQ)

- `appointments (scheduled_date, status)` — calendar FULL computation and daily lists.
- `clinic_visits (status, created_at)` — Live Queue polling every 3–5 s.
- FK columns indexed per Laravel convention.

### 6.5 Seed data

Seeders shall provide: the 11 colleges (D-43); 1 director, 1 nurse, 11 college admins (one per college); a realistic demo cohort of students across colleges with profiles + QR tokens — every student carrying a program their college actually offers and a year-level **key** from `config/programs.php` (D-42), since a seeded profile is re-validated against that catalog the moment the student edits it; and (for W10 demo prep) a seeded history of encoded visits spanning all 8 case categories and both sexes so analytics render meaningfully.

### 6.6 Data retention & privacy posture

Student health data never leaves the server (no third-party APIs). Access is least-privilege by role; students see only their own records; admins see only their college's *roster and batch* data (never clinical results); the nurse and director see clinical data as required by their function. Deletion/retention policy beyond the capstone demo is institution-defined and out of scope, but the schema supports it (cascading rules to be conservative: restrict deletes on users with clinical records).

---

## 7. Non-Functional Requirements (ISO/IEC 25010:2023 mapping)

| # | Quality characteristic | Requirement | Target / verification |
|---|---|---|---|
| NFR-1 | Functional suitability | The clearance workflow produces a correct, printable result per student matching the official PamSU form | SM-3, SM-7 |
| NFR-2 | Performance efficiency | Kiosk step transitions feel immediate (< 300 ms UI response); queue reflects submissions within one poll cycle | SM-2; manual timing |
| NFR-3 | Compatibility | Kiosk: Chromium kiosk mode on Raspberry Pi OS. Web app: current Chrome/Edge/Firefox on desktop. **Web Serial is Chromium-only and only promised on the kiosk.** | Browser matrix smoke test |
| NFR-4 | Interaction capability | Kiosk is touch-first at 1080×1920 portrait (D-26): ≥ 48 px touch targets, on-screen keyboards for all text/number input, no hover-dependent UI *(display presentation per D-18: responsive fill + `--k-zoom`)* | UAT with non-team users |
| NFR-5 | Reliability | Captured visits survive crashes/restarts until encoded; kiosk auto-resets between students; sensor failure never blocks a session (manual fallback) | SM-4; pull-the-plug tests |
| NFR-6 | Security | RA 10173 consent at registration and per kiosk session; RBAC middleware; bcrypt password hashing; server-side college scoping; CSRF protection; Web Serial confined to localhost; no public exposure of health data | SM-5; negative-path test suite |
| NFR-7 | Maintainability | One Laravel codebase; shared Blade components (HPButton, HPCard, HPBadge, HPInput, HPSelect, HPTextarea, HPLogo, SidebarLayout); thresholds and capacity centralized in `config/healthpass.php`; Git feature-branch + PR workflow | Code review checklist |
| NFR-8 | Portability | Runs on XAMPP (Windows dev), on the Pi (demo), and on a PHP 8.2+/MySQL host (deployment) with `.env`-only changes | Deployment dry-run (W12) |
| NFR-9 | Flexibility (sub-char.) | Capacity, thresholds, clinic hours adjustable without code edits to views/controllers (config-driven) | Change-one-value test |
| NFR-10 | Interaction capability (accessibility) | **FR-UI-04 — Light/dark theme toggle (D-38).** Every web-app page carries a theme toggle in the sticky header (guest/auth pages: fixed top-right). The choice persists per device in `localStorage` (`hp-theme`), defaulting to the OS `prefers-color-scheme` on first visit; there is **no schema change, no user column and no request on toggle**. The theme class is applied by an inline `<head>` script before first paint (no flash on reload). Colour transitions use the `--hp-dur-base` / `--hp-ease-out` motion tokens and honour the existing `prefers-reduced-motion` kill switch. All dark-theme **text** meets WCAG 2.1 AA (4.5:1 body / 3:1 large), except the pre-existing brand-locked white-on-orange button label. **Excluded: `resources/views/kiosk/**` and all print views**, which stay light. | Automated per-page contrast audit across all four roles in both themes; kiosk + print asserted light while dark is active |

---

## 8. UI / UX Requirements

### 8.1 Design system (source of truth: Claude Design HTML prototypes, June 2026)

**Light palette (default — unchanged):**

| Token | Value | Usage |
|---|---|---|
| White | `#FFFFFF` | Cards, inputs, sidebar |
| Background | `#F6F2ED` | App and kiosk backgrounds |
| Peach | `#FFCAA0` | Active nav, badge fills, selected states |
| Orange | `#FF8C2A` | Primary buttons, active text, logo accent |
| Slate | `#4B5563` | Body text, icons, borders |

**Dark palette (D-38 — web app only):** the same five token *names* are re-pointed;
no view knows a second theme exists. Measured contrast is given against the dark
card surface `#1A1D24`.

| Token | Dark value | Usage | Contrast |
|---|---|---|---|
| White (surface) | `#1A1D24` | Cards, sidebar, header, inputs | 1.12:1 vs page bg — separated by the border token, not by luminance |
| Background | `#0F1115` | Page background | — |
| Peach | `#3B2A17` | Active nav, badge fills, selected chips | **adjusted** — see below |
| Orange | `#FF8C2A` | Primary buttons, active text, logo accent | **7.3:1** on card — AA for body text |
| Slate (text) | `#F3F4F6` | Body text, icons, borders (via alpha) | **13.0:1** on card |

Two adjustments were made to the proposed dark palette, both verified with a
WCAG 2.1 relative-luminance calculation rather than estimated:

- **Peach `#FFC89A` → `#3B2A17`.** The app pairs `bg-hp-peach` with
  `text-hp-orange` everywhere (active nav item, `soft` button, positive/approved/
  fit badges). Orange on a *light* peach measures **1.55:1** — unreadable. Keeping
  peach as the chip surface but darkening it inverts the pair correctly: orange on
  `#3B2A17` measures **5.9:1** (AA for body text) with no per-component overrides.
- **Slate `#E5E7EB` → `#F3F4F6`.** Roughly 300 existing utilities are
  `text-hp-slate/NN` at alphas tuned for a light background; on a dark surface the
  same alpha composites *toward* the background and loses contrast. The brighter
  base plus a dark-only alpha bump (see D-38) lifts every muted-text step to ≥ 4.8:1.

**The orange did NOT need a lighter variant for small text.** `#FF8C2A` on
`#1A1D24` measures **7.3:1**, not the ~3.3:1 that was assumed — dark mode is where
brand orange performs *best*. The genuinely failing pair was orange-on-peach, fixed
above. The one remaining sub-AA pair, **white on orange (2.32:1)** — the primary
button label — is **pre-existing, identical in the light theme, and brand-locked**;
it is out of scope for a dark-mode change and is recorded here as a known issue.

Typography: **Poppins** 400/500/600/700 (Google Fonts). Body 13–14 px/400, labels 600, headings 700. Scrollbars 5 px slate at ~16% opacity. Implementation: CSS variables (`--orange: #FF8C2A` etc.) lifted directly from the prototype; Tailwind utilities may complement but never override the tokens.

**Theme exclusions (D-38): the kiosk and every print view stay permanently light.**
The kiosk is a fixed clinic appliance tuned for standing-distance readability on a
1080×1920 panel (D-26) and is auth-less, so it has no toggle and never receives the
theme class. Printed clearance output must match the official form regardless of
screen theme.

### 8.2 Shared Blade components (build first — every screen depends on them)

`HPButton` (pill r999; variants primary/ghost/soft/muted; sizes sm–xl; disabled 0.5 opacity) · `HPBadge` (pill 11px/600; peach+orange for positive/flagged/approved/fit/live, slate-tint for neutral/pending/rejected/unfit; `live` solid orange) · `HPCard` (white, r12, 1px slate-15 border, 24px padding) · `HPInput`/`HPSelect`/`HPTextarea` (r8, 1.5px slate-25 border; password eye toggle) · `HPLogo` (orange plus-cross + "Health"/"Pass" wordmark) · `SidebarLayout` (220px sidebar, role-specific nav, 56px header, scrollable main on `#F6F2ED`).

Icons: Lucide-style outline set (24px viewBox, stroke 2) — Home, Calendar, FileText, QrCode, Plus, List, Activity, Edit, BarChart, Alert, Check, Chevrons, X, Search, LogOut, Download, Monitor, Users, Eye/EyeOff.

### 8.3 Screen inventory & navigation

| Role | Nav / screens |
|---|---|
| Public | Login · Register (4-step wizard) |
| Student | Dashboard · Book Appointment (+ confirmation) · My Records (+ record modal) · My ID & Profile (+ edit modal) · Kiosk Tutorial (FR-STU-11) |
| College Admin | Dashboard · New Batch Request (+ confirmation) · Batch Tracking |
| Nurse | Live Queue · Encode Result (+ print preview iframe) · Enable Kiosk Mode |
| Director | Dashboard · Batch Approvals · Analytics · Flagged Anomalies |
| Kiosk (`/kiosk`) | Welcome · Email Login · Identity Confirm · Walk-in Check (No Scheduled Clearance — FR-KSK-03a) · Privacy Consent · Vitals ×4 · Questionnaire · Review · Complete |

The page-by-page behavioural spec in `HealthPass_Context.md` §7 (field lists, button states, empty/disabled rules, exact copy) is **incorporated by reference** and is binding for implementation.

### 8.4 Kiosk-specific UX rules

Responsive fill + `--k-zoom` scaling (D-18) on a **1080×1920 portrait** display (D-26 — supersedes the original 800×480 fixed canvas / dark letterbox; screens stack in a single column); 3-phase vital capture (ready → scanning → captured) with progress dots; virtual QWERTY + numeric pads for all input; large hit areas sized for a 15.6″ panel used standing; "Not you?" and "Decline" always escape to Welcome; 12 s completion auto-reset; 90 s idle reset (FR-KSK-15); no browser chrome, no navigation out of the flow.

---
## 9. Technical Architecture

### 9.1 Topology

A **single unified Laravel application** — no separate API service, no microservices, no cross-system tokens. The kiosk is the route `/kiosk` in the same app, opened full-screen by Chromium on the Raspberry Pi.

```
                 ┌──────────────────────────────────────────────┐
                 │           HealthPass — Laravel app            │
                 │      (Blade + Controllers + MySQL/Eloquent)   │
   Browsers ───► │  /login /register /student/* /admin/*         │
 (staff +        │  /nurse/* /director/*                         │
  students)      │                                                │
                 │  /kiosk ◄── full-screen Chromium on the Pi     │
                 └───────────────┬────────────────────────────────┘
                                 │ Web Serial API (kiosk page JS)
                                 ▼
                 ┌──────────────────────────────────────────────┐
                 │ Arduino / ESP32 over USB — combined reading   │
                 │ Sensors: ultrasonic (height), HX711 (weight), │
                 │ MLX90614 (temp), serial BP monitor (BP + HR)  │
                 └──────────────────────────────────────────────┘
```

### 9.2 Stack (locked)

| Concern | Choice |
|---|---|
| Framework | Laravel 11/12, PHP 8.2+ |
| Views | Blade + shared Blade components (§8.2) |
| Database | MySQL (XAMPP dev; hosted MySQL at deployment) |
| Auth | Laravel Breeze (Blade stack), role middleware on top |
| Styling | Prototype CSS variables + Poppins; Tailwind as utility layer |
| Charts | Chart.js (Director analytics) |
| QR — generation | `simplesoftwareio/simple-qrcode` — generates each student's kiosk QR from `qr_token` |
| QR — kiosk read | USB keyboard-wedge scanner — "types" the ID's multi-line QR text + Enter into the Welcome screen's focused input; the kiosk normalizes by extracting the `IDNo:` line's value if present, falling back to the full string for single-token backup QRs |
| QR — registration & late-linking capture | `html5-qrcode` (client-side JS) — camera or uploaded ID photo, decoded in-browser; `IDNo` extracted and matched to `student_number` before POSTing |
| Printing | Blade print view in iframe + `window.print()`; dompdf optional later |
| Queue refresh | Polling (`setInterval` + `fetch`, 3–5 s) — WebSockets deliberately excluded |
| Mail | `MAIL_MAILER=log` / Mailtrap in dev; real SMTP at deployment |
| VCS | Git + GitHub (`Nat-G1t/Healthpass`), feature branches + PRs |

### 9.3 Application conventions

Role-prefixed route groups with middleware; controllers per module mirroring §4 (Auth, Registration, Student, Admin, DirectorApprovals, Kiosk, NurseQueue, Encode, Print, Analytics); Eloquent models matching §6; all thresholds/capacity/hours in `config/healthpass.php`; reference-number generation in one service class; DB transactions around batch approval (FR-DIRA-02) and kiosk submit (FR-KSK-12).

---

## 10. Environments & Deployment

| Environment | Where | Notes |
|---|---|---|
| Development | Windows + XAMPP, `C:\Capstone\healthpass` | MySQL via `127.0.0.1` (never `localhost` in `.env`); run `php artisan serve --port=8080` and `npm run dev` in parallel terminals; kiosk + Web Serial testable in Chrome at `http://127.0.0.1:8080/kiosk` with the MCU on the laptop (`127.0.0.1` is also a secure context) |
| **Demo / defense — PRIMARY (D-34)** | **One hosted app over HTTPS** (VPS + domain + Let's Encrypt); Chromium on the Pi opens `https://<domain>/kiosk`; all staff and students use the same public site from their own devices | HTTPS = secure context → Web Serial works. Requires `HEALTHPASS_KIOSK_ALLOW_LOOPBACK=false`, `TRUSTED_PROXIES` set to the real proxy IP, the Pi enrolled as a kiosk device (D-27), and the ESP32 serial grant re-issued on the new origin (grants are per-origin). Runbook: `docs/deployment-hosted.md` |
| Offline fallback (was D-9) | Laravel runs on the Raspberry Pi itself; Chromium opens `http://localhost/kiosk`; staff reach it over the campus LAN | localhost = secure context → Web Serial with no TLS. Retained and **rehearsed** as the defense-day contingency if venue internet fails; switch is `KIOSK_URL` + `ALLOW_LOOPBACK=true` + `config:cache`. See `docs/deployment-pi.md` §10 |

Even without AI, HealthPass processes student health data and remains an **RA 10173 (Data Privacy Act of 2012)** system: consent capture stays mandatory, access is role-restricted, and no health data is exposed publicly or sent to third parties.

---

## 11. Hardware Specification (Kiosk)

### 11.1 Bill of components

| Component | Item | Role |
|---|---|---|
| Host | Raspberry Pi 4 + 15.6″ 1080×1920 touch display, portrait (D-26) | Runs Chromium in `--kiosk` mode pointed at `http://localhost/kiosk`; display rotated to portrait at the OS level (see `docs/deployment-pi.md`) |
| Sensor hub | Arduino or ESP32 | Aggregates all sensors; one USB serial line to the Pi |
| Height | Ultrasonic distance sensor | Mounted overhead; distance → height |
| Weight | Load cell + HX711 amplifier | Floor platform |
| Temperature | MLX90614 IR thermometer | Non-contact forehead read |
| BP + HR | Digital BP monitor **with serial output** | The single riskiest part — must be validated in the Week-1 spike **before purchase** (FR-HW-04) |
| ID login | USB QR/barcode scanner (keyboard-wedge) | Types `qr_token` + Enter into the focused field |

### 11.2 Serial message contract (agreed Week 1, owned jointly by both developers)

One line, ASCII, newline-terminated, e.g.:

```
H:163;W:64;T:37.9;BP:145/92;HR:78
```

Keys: `H` height cm (int), `W` weight kg (int or 1-dec), `T` temperature °C (1-dec), `BP` systolic/diastolic mmHg (ints), `HR` bpm (int). Partial lines (e.g., only `H:` during the height step) are valid; the kiosk parser consumes whichever keys are present and ignores unknown keys (forward compatibility). Malformed lines are dropped silently with a retry, never crash the step.

### 11.3 Web Serial constraint (architecture-shaping)

Web Serial requires a **secure context**: `https://` or `http://localhost`. Hence the app runs **on the Pi** and the kiosk opens localhost (D-9). If the server were a separate machine, the kiosk would need HTTPS (self-signed at minimum) — avoided for the defense. Web Serial is **Chromium-only**; the kiosk officially supports Chromium and nothing else.

---

## 12. Release Plan & Milestones (12 weeks, 4 sprints)

| Sprint | Weeks | Application track (Nat) | Hardware track (Baldo) | Exit criteria |
|---|---|---|---|---|
| **S1 — Foundation** | W1 (Jun 8–14) | Env setup, Git, Breeze; all 10 migrations from §6; vertical slice login → student dashboard | **Web Serial spike**: one sensor reading in Chromium on the Pi; BP monitor serial confirmed; *purchases gated on this* | Slice demo + spike verdict at W2 adviser check-in |
| | W2 (Jun 15–21) | Roles/middleware; 4-step registration (consent → info → OTP → QR) | Height + weight wired; BMI computed; serial format settled | Register + login as all roles |
| | W3 (Jun 22–28) | Student booking + records + profile/My ID | Temperature sensor; reading repeatability tests | Student can book against capacity rules |
| **S2 — Kiosk** | W4 (Jun 29–Jul 5) | Kiosk UI ported to Blade (all screens) **with manual entry on every vital step** | BP + HR added; full combined reading handed off | Kiosk completable end-to-end on manual entry alone |
| | W5 (Jul 6–12) | Web Serial parsing into vital steps; QR/USB login; submit creates visit + 1:1 rows | On-Pi integration in true kiosk mode | Sensor-driven kiosk session submits to DB |
| | W6 (Jul 13–19) | Nurse Live Queue (polling) + flag computation/badges | Kiosk-mode reliability: reconnects, idle, unattended quirks documented | Submission visible in queue ≤ 5 s |
| **S3 — Clinical loop + cohorts** | W7 (Jul 20–26) | Encode Result + official print view | Physical enclosure, mounting, cabling, power | Printed form matches official (SM-3 sign-off) |
| | W8 (Jul 27–Aug 2) | College Admin batch workflow (college-scoped) | Joint dry-run #1; integration fixes | Batch submit → pending visible to Director |
| | W9 (Aug 3–9) | Director approvals (transactional appointment generation) + analytics start | Analytics data support; reliability testing | Approve → N appointments atomically |
| **S4 — Analytics, hardening, freeze** | W10 (Aug 10–16) | Analytics finished (matrix, charts, flags, export) + realistic seed data | Final hardware hardening; spares procured | Director suite demo-ready |
| | W11 (Aug 17–23) | **Feature freeze.** Full integration testing, ISO 25010 evaluation runs, bug fixes | Clinic-floor rehearsal under realistic conditions | All SM-1…SM-8 measured |
| | W12 (Aug 24–30) | Polish, docs, deployment dry-run, defense rehearsal; **system done Fri Aug 28**, 2-day buffer | Defense-day hardware checklist + backup plan | Dress rehearsal passes |

Non-programmer team across all sprints: paper title/scope revision (R-6), test cases + UAT from ~W6, ISO 25010 instrument prep, clinic coordination & consent logistics. Adviser check-ins (A. Viscayno): end of W2 / W5 / W8 / W11.

**Slip policy:** if scope must be cut under pressure, the **batch workflow (Modules ADM + DIR-A)** is the designated slip candidate — it is the least essential to the core clearance loop. The kiosk, queue, encode, and print loop is never cut.

---

## 13. Acceptance & UAT

1. **Requirement-level:** every Must FR in §4 verified against its module AC by the QA sub-team, tracked in a traceability sheet (FR ID → test case ID → pass/fail → tester → date). Test-case authoring starts W6.
2. **End-to-end scenarios (W11):**
   - E2E-1 Self-booked medical clearance: register → book → kiosk (sensor) → queue → encode Fit → print → record visible to student.
   - E2E-2 Walk-in with flags: no booking → kiosk with feverish/high-BP seeded values → flags in queue and Director anomalies → encode Unfit.
   - E2E-3 Batch cohort: admin selects 20 students → Director approves with date → 20 appointments exist → 3 sampled students complete the loop.
   - E2E-4 Degraded hardware: MCU unplugged mid-vitals → manual completion → submission integrity intact.
   - E2E-5 Security negatives: cross-role URLs, cross-college tampering, direct POSTs without CSRF — all rejected.
   - E2E-6 Dental: booking only; verify it never enters the kiosk/encode loop.
3. **Form fidelity sign-off:** clinic staff compare a printed system clearance with the blank official form (SM-3).
4. **ISO/IEC 25010 evaluation:** instrument administered per the team's methodology chapter; results feed the capstone paper.

---

## 14. Decisions Log (locked — change requires team + adviser sign-off)

| ID | Decision | Rationale |
|---|---|---|
| D-1 | **No AI / predictive component** | Scope realism; clinical judgment stays human; removes Azure dependency and pseudonymization burden |
| D-2 | **No Doctor role** — nurse encodes; physician pre-printed on form | Matches actual clinic operation; 4 roles keep RBAC simple |
| D-3 | **Dental = scheduling only** | Only Medical Clearance needs the kiosk → encode → print loop |
| D-4 | **Clinic day capacity = config value** (`config/healthpass.php`), counted against non-cancelled appointments | Zero extra tables; an admin-editable settings table later is additive, not breaking |
| D-5 | **Director picks the appointment date at batch approval** (default today); stored in `batch_requests.scheduled_date` and stamped on generated appointments. **Amended by D-29 (v1.8):** the date now originates from the College Admin at submission; the Director confirms or adjusts it — approval still stamps the final date into `scheduled_date`. | Single decision point; simplest defensible flow; one nullable column |
| D-6 | **Respiratory rate not captured**; blank on the printed form | Sensor placement constraints (decided in prior iteration); form allows blank |
| D-7 | **Manual entry is a first-class path on every vital step**; sensors are progressive enhancement; provenance in `vital_signs.entry_method` | De-risks defense day; LLM-council recommendation; cheap insurance the panel reads as good engineering |
| D-8 | **Registration OTP via hashed cache entry (10-min TTL)** — no OTP table | Matches the prototype's 6-digit UI without schema impact; Breeze's link-based verification kept as documented fallback if OTP proves troublesome |
| D-9 | **Laravel runs on the Pi; kiosk at `http://localhost`** | Satisfies Web Serial's secure-context rule with no TLS; staff use LAN — ⚠️ **superseded by D-34** (hosted HTTPS is now the primary shape; this becomes the documented offline fallback). Row kept for the audit trail. |
| D-10 | **BP flag threshold = systolic ≥ 140 OR diastolic ≥ 90** | Resolves the 130/85 vs 140/90 document discrepancy; aligned with stage-2 hypertension screening convention; canonical in config |
| D-11 | **Polling, not WebSockets**, for the live queue | Single-clinic scale; massive complexity saving for two Laravel beginners |
| D-12 | **Kiosk ID login uses a USB keyboard-wedge QR scanner** as the primary input path; email + virtual keyboard is the fallback. The kiosk normalizes multi-line QR payloads (format from physical DHVSU IDs) by extracting the `IDNo:` line's value; falls back to the full string for single-value tokens (phone-QR backup). | Far more reliable for unattended kiosk conditions than camera decode; the normalization step keeps both real-ID and phone-QR paths working against one `qr_token` field. |
| D-13 | **Live Queue is FIFO (oldest first)** — top row = next to serve ("NEXT" badge); new arrivals append at the bottom | Matches real clinic fairness (first come, first served); the prototype's emphasized row-1 styling maps cleanly to "next to serve" |
| D-14 | **Registration (Step 4) and late-linking (My ID) use in-browser camera or photo upload** for QR capture, decoded client-side by `html5-qrcode`; only the extracted `IDNo` is POSTed and stored as `qr_token`. | Students register on personal phones/laptops — a USB scanner is not available there. Camera/upload is the only viable primary path on personal devices. |
| D-15 | **Booking available on weekends (clinic open daily)** — past dates still unbookable; capacity rule unchanged | Scope change approved by team/adviser |
| D-16 | **Kiosk surfaces a walk-in confirmation gate** (FR-KSK-03a) after Identity Confirm and before Privacy Consent, shown only when the student has **no non-cancelled appointment dated today at all — medical *or* dental**. Submit-time `appointment_id` linkage (FR-KSK-12 / BR-10) stays **medical-only** and unchanged. | Sets the student's expectation up front (anything booked today vs. nothing) without altering how the visit links to an appointment. A dental booking suppresses the "no clearance" notice but does not link at submit — dental stays scheduling-only (D-3), so a dental-only student records as a walk-in. |
| D-17 | **Student college is self-editable; current `college_id` drives all forward-looking behavior, a per-visit snapshot freezes history.** Changing a student's college re-scopes them live: future kiosk visits and College-Admin student lists/batch eligibility follow the current `student_profiles.college_id`, so admin reassignment is automatic (no manual move). Historical analytics stay frozen because each `clinic_visits` row snapshots `college_id` at submit (FR-KSK-12); FR-ANL-02/03/05/08 read that snapshot, so a transfer never re-attributes past cases or flags. | One editable field serves both "where the student is now" and "where they were screened" without a separate transfer table or history log. |
| D-18 | **Kiosk display = responsive fill + `--k-zoom` scaling, superseding the fixed 800×480 letterbox.** The `.kiosk-panel` fills the viewport (`inset: 0`, no dark bars on any screen) and a single `--k-zoom` CSS var (default `1.3`) scales every rem-based size for 7″ readability. The **800×480 prototype canvas remains the design reference** (design system, §4.6, §8.4, NFR-4) and the **hardware still renders at 800×480**, so layout fidelity is preserved — only the letterbox-with-bars presentation is replaced. | Real hardware showed black bars and small type at a true fixed letterbox; fill + one zoom var reads 1:1 on the 800×480 target while degrading gracefully on any dev/preview viewport, with no per-breakpoint layout work. |
| D-19 | **FR-KSK-16 staff exit is implemented as full email + password nurse authentication**, not a bare password prompt. The discreet exit gesture opens a nurse login; on success the redirect lands directly in the **authenticated nurse queue** (not merely back to a public page). | A real authentication establishes the nurse session the queue requires, so exit + resume-work is one step; a bare shared password would neither identify the nurse nor satisfy the queue's auth guard. |
| D-21 | ⚠️ **PENDING ADVISER SIGN-OFF** — **Same-day booking closes at the clinic closing hour (BR-20).** Once the local Asia/Manila clock reaches `config('healthpass.closing_hour')` (default 17 / 5:00 PM, kept in sync with `clinic_hours.close` and BR-01), students can no longer self-book **today**; a modal recommends the next day. The cutoff is enforced server-side (`StoreAppointmentRequest` rejects the write; the availability endpoint greys today out) — the browser clock is never trusted. This also required setting `config('app.timezone')` from the framework default `UTC` to **`Asia/Manila`**, which affects all app timestamps. | Prevents students booking a slot for a clinic that has already closed for the day, without a per-slot time-of-day scheduling model; one config hour drives both the validation and the calendar. Timezone fix makes "today"/"now" comparisons meaningful for a single-campus PH clinic. |
| D-20 | ⚠️ **PENDING ADVISER SIGN-OFF** — **Password recovery & password change use the D-8 OTP pattern**, replacing Breeze's emailed reset link (amends FR-AUTH-09). Forgot-password: email → OTP → new password, with a generic anti-enumeration response. Change-password (all roles, sidebar entry): the new hash is staged in cache and applied only on OTP confirmation; the staged hash expires with the 10-min TTL. Breeze's link-based reset controllers/views/routes and the direct (non-OTP) profile password form were removed. All OTP screens (registration Step 3, student email change, both password flows) share a 60-second server-enforced resend cooldown. | One OTP mechanism across the whole app (students already know it from registration); no reset-token URLs to intercept or leak via email forwarding; the staged-hash design guarantees an abandoned flow never half-changes a password. Recorded here because FR-AUTH-09 previously locked reset to Breeze defaults — this row is the flagged change request, not a silent rewrite. |
| D-21 | ⚠️ **PENDING ADVISER SIGN-OFF** — **A student-facing Kiosk Tutorial page is added to the web app** (FR-STU-11): a 7-step walkthrough of the kiosk flow in the student nav, plus a dismissible first-booking prompt on the booking confirmation screen. Student-facing only (other roles operate the kiosk or never use it, so no tutorial in their navs). Media slots ship as styled GIF placeholders until the hardware track records real kiosk footage. | Onboarding: most students meet the kiosk exactly once a year; a 2-minute preview reduces at-kiosk hesitation and nurse hand-holding. New screen was not in the original §8.3 inventory — recorded here as a flagged scope addition, not a silent one. |
| D-22 | **Physical Signs Disorder is the physician's in-clinic examination, recorded by the nurse on the encode screen.** Nine optional YES/NO rows sit in the Doctor's Assessment card before Nurse Notes (stored as nine nullable boolean `ps_*` columns on `clearance_records` — **flagged schema change**, data dictionary updated); the printed form shades those bubbles, an unanswered row prints blank. The **pregnancy/LMP line pre-fills from the kiosk questionnaire** (the student already answered it, FR-KSK-10). **Student No. is not printed** (the official form has no such field) and **Case Category is not printed** — the physician hand-writes case details under REMARKS based on the shaded YES physical signs. The kiosk's 9-system questionnaire never prints — it is reference for the nurse on the encode screen only (its self-reported systems are not the form's exam list); a questionnaire **YES pre-checks the matching exam row** (skin→SKIN, digestive→ABDOMEN (GIT), nose→HEENT, respiratory→CHEST/LUNGS, bones→EXTREMITIES, heart→HEART/CVS, nervous→NEUROLOGICAL) for the nurse to confirm or correct after the exam — a kiosk NO never pre-fills, so an unexamined row still prints blank *(pre-fill scope amended by D-25, v1.5: kiosk NO answers now pre-check NO as well)*. The vision/hearing answers have no exam row: they are decision support for the "Eyes, Ears, Nose & Throat Disorders" case-category pick (D-23). Amends FR-NRS-03 and FR-PRT-02 (v1.4). | Matches the real clinic workflow on the official form (SM-3): the physician examines and the nurse transcribes; only exam findings and the student's own pregnancy answer belong on the signed document, while self-reported kiosk answers inform the assessment — a self-reported YES is exactly what the physician will look at first, so it opens pre-checked. |
| D-23 | **A clearance can carry multiple medical-system case categories.** The encode screen's category picker becomes a multi-select (checkboxes, zero or more of the locked 8-list); each selection persists as one row in the new **`clearance_case_categories` child table — table #11, a flagged extension of the originally locked 10-table schema** (the old single-value `clearance_records.case_category` column was migrated in and dropped). Cases-per-category analytics (FR-ANL-02/03, monthly summary) count one per record × category via a plain JOIN; the student's My Records modal chips every category on the case. | Real cases span systems (e.g. respiratory + cardiovascular) and the single enum forced a lossy pick. A child table, not a JSON column, keeps the category aggregations portable SQL (MySQL dev / SQLite tests — the repo bans engine-specific raw SQL) and dedupes per record via a composite UNIQUE. |
| D-24 | **"Others, Specify" purpose (official-form parity) — flagged schema change.** The encode screen's Purpose dropdown gains the form's fifth line, **Others, Specify…**; picking it reveals a required free-text input for the exact event (max 120 chars). `clearance_records.purpose` changes **enum → varchar(50)** (the app-side `Rule::in` validation remains the real gate, as it always was on SQLite) and a new **`purpose_other` varchar(120) NULL** column stores the event. A stray specify text is cleared server-side when a listed purpose is picked. The printed form shades the "Others, Specify:" bubble and prints the event on its line — **clipped, never wrapped**, so a long event cannot break the one-page fit (FR-PRT-05). `ClearanceRecord::PURPOSES` stays the locked 4-list; Others is a separate `PURPOSE_OTHERS` constant so loops and any future purpose analytics keep the two apart. Amends FR-NRS-03, FR-PRT-02, BR-16. | The official form itself carries the "Others, Specify: ___" line — the locked 4-list physically could not record real one-off events (competitions, defenses, off-campus ceremonies), forcing wrong-bucket picks. |
| D-25 | **Form-accuracy refinements: no college on the printout; kiosk NO answers pre-fill; Letter default.** (a) The printed **Course, Year & Section** line carries course + year only — the college is **not** printed anywhere (the official form has no college field; this drops the v1.4 "college rides the course value" divergence). College remains on the encode screen and in the capture-time analytics snapshot (D-17) — only the printout changes. (b) The encode screen's physical-sign rows now pre-check the student's kiosk answer **both ways — YES and NO** — for the nurse to confirm or correct after the exam; rows without a kiosk counterpart (GUT, BREAST) still open unanswered and print blank. Partially supersedes D-22's "a kiosk NO never pre-fills". (c) The print template's default paper is **Letter** (`@page size: letter`); the layout is sized to fit one page on Letter *and* A4, so a dialog override still prints clean. Amends FR-PRT-02, FR-NRS-03/D-22. | Clinic-review feedback: the college on the course line diverged from the official form for no operational gain; the student already answered the 9-system questionnaire, so an all-blank NO column made the nurse re-enter known answers row by row; Letter matches the clinic's paper stock. |
| D-27 | **Kiosk device authorization + loopback config switch (security upgrade) — flagged schema change (table #12, `kiosk_devices`).** `KioskAccess` now admits a /kiosk request when ONE holds: (a) it carries a valid, un-revoked **device token** — a browser a nurse enrolled via "Enable Kiosk Mode" (FR-NRS-06); the token rides a long-lived HttpOnly, SameSite=Lax cookie (Secure on HTTPS) or is provisioned once via `?device_token=…` (validated, cookie-set, then redirected to a clean URL — for the Pi's incognito launcher, which wipes cookies each start, so the token lives in `KIOSK_URL`); only a **SHA-256 hash** of the token is stored, never the plaintext; (b) an authenticated **active nurse**; or (c) **loopback AND `config('healthpass.kiosk.allow_loopback')`** (new `HEALTHPASS_KIOSK_ALLOW_LOOPBACK`, default **true** for the Pi-local shape, **false** for the hosted internet deploy — never keyed on APP_ENV; closes the "misconfigured reverse proxy reports 127.0.0.1 for every visitor" hole). Everyone else (logged-in student/admin/director, or a guest) gets a **friendly branded 403 page** with a link back to their dashboard/login — not a bare stub or a login redirect. Dedicated per-endpoint **throttle prefixes** on every kiosk POST (scan/login/submit/reset/exit) so one endpoint can't exhaust another's shared per-IP bucket. The person at the terminal still never logs in — the **DEVICE** is trusted, never client-supplied identity (existing kiosk trust rules unchanged). Amends FR-NRS-06; the internet deploy must also set Laravel **trusted proxies to the real proxy IPs only — never `*`**. | The kiosk was reachable by any authenticated nurse or loopback only; enrolling specific devices lets a staff terminal run the kiosk unattended without a standing nurse login, while the hashed-token + revocation model keeps it auditable and revocable. The loopback switch and trusted-proxy rule harden the hosted shape against IP spoofing; the branded 403 replaces a bare stub for a page students will occasionally hit by mistake. |
| D-26 | **Kiosk display = 15.6″ 1080×1920 portrait touchscreen**, replacing the 7″ 800×480 landscape panel (already working on the Pi). All kiosk screens reworked for portrait: single-column stacking (Welcome scan/email, email-login and staff-exit fields, Review's two cards — all previously side-by-side), `--k-zoom` retuned from 1.3 to **2** (vitals 1.69 → **2.25**), and touch targets/spacing enlarged for a standing user. The D-18 responsive-fill + `--k-zoom` model is unchanged — only the target resolution, orientation, and per-screen compositions moved; state machine, flow, and endpoints untouched. The Pi must rotate the display to portrait at the OS level (Wayland/labwc — e.g. `wlr-randr --output HDMI-A-1 --transform 90`; exact config owned by the hardware track, `docs/deployment-pi.md` §4). Supersedes every 800×480 / 7″ reference (§2 out-of-scope, §4.6, NFR-4, §8.4, §11.1, design system). | The 7″ panel was hard to read and tap at kiosk distance; the larger portrait panel matches how a standing student actually faces the terminal, fits the full vitals card + keyboard without the old compact spacing, and was validated working on the Pi before the rework. |
| D-28 | **Purpose of Medical Clearance is chosen by the student at self-booking — flagged schema change (`appointments` gains `purpose`/`purpose_other`).** The Book Appointment page gains a third card, **Purpose of Medical Clearance**, for Medical bookings only (Dental is scheduling-only, no clearance form): the same locked dropdown as nurse encode — the four `ClearanceRecord::PURPOSES` plus **Others, Specify** free-text (the shared `<x-hp.purpose-fieldset>` Blade component, extracted so encode and booking cannot drift). Two new columns on `appointments`: **`purpose` varchar(50) NULL** (mirrors `clearance_records.purpose` — string, app-side `Rule::in` is the gate) and **`purpose_other` varchar(120) NULL**. The booking Form Request validates server-side: purpose `required_if:service,medical`, from the locked list; `purpose_other` required + max:120 when Others; purpose is normalized to NULL for dental and stray specify text dropped for a listed purpose — never trusting the client. **Fallback rule:** the nurse encode screen shows its own Purpose dropdown **only when the visit's appointment carries no student-supplied purpose** (walk-in kiosk visits, batch-booked students, and any appointment made before this feature); when the appointment DOES carry a purpose, encode hides the input, shows the student's choice read-only, and `EncodeController` copies `purpose`/`purpose_other` onto the clearance record on Save — so the printed form (FR-PRT-02) auto-populates with **zero print-template changes**. Amends FR-STU-02/04, FR-NRS-03, BR-16. | The purpose is the student's own intent (why they need the clearance), known at booking — capturing it up front removes a nurse data-entry step and a transcription error, and lets batch/walk-in cases still fall back to the nurse dropdown. A string column + `Rule::in` (not a DB enum) matches the existing `clearance_records.purpose` decision (D-24) and keeps SQLite/MySQL portable; a shared Blade component keeps the two dropdowns identical by construction. |
| D-29 | ⚠️ **PARTLY SUPERSEDED BY D-36 (v1.13) — the "Director may adjust" clause is dead; everything else stands.** **Batch clinic date is admin-proposed, Director-confirmed — flagged schema change (`batch_requests` gains `requested_date` DATE NULL).** The New Batch Request form gains a required **Requested Clinic Date** picker (default today, past dates disallowed, server-validated), stored in `requested_date` at submission. ~~The Director's approve modal **pre-fills with that date** and the Director may adjust it — when the requested date passed while the batch sat pending the picker falls back to today with an explicit stale-date notice~~ (**D-36:** the modal now shows the date read-only and offers no input; a stale requested date **blocks approval outright** and is grounds for rejection, not adjustment), and the FR-DIRA-06 capacity warning still applies. Approval continues to stamp the **final** date into `scheduled_date` (D-5's stamping mechanics unchanged); `requested_date` is NULL only on batches submitted before this change, ~~for which the modal simply defaults to today~~ (**D-36:** those batches can no longer be approved at all). Amends FR-ADM-02/04, FR-DIRA-01/02; partially supersedes D-5 (origin of the date, not its stamping). Row kept for the audit trail. | The College Admin knows when the cohort's event (OJT start, graduation, field trip) happens — the Director doesn't, so a director-invented date was guesswork. Keeping the Director as confirmer preserves D-5's single decision point for clinic-load balancing (capacity warning, adjust-don't-block) while the date's origin moves to the person who actually has the information. |
| D-30 | **The FR-ANL-03 Summary of Medical Cases matrix renders inside the FR-ANL-02 chart's "View as table" toggle** on the Analytics screen, replacing the chart's original college-rows fallback table — not as a separate always-visible card. Both are the same encoded college × category dataset (one grouped query feeds both), so the matrix now doubles as the chart's accessible/contrast fallback. The matrix content itself is unchanged from FR-ANL-03 (8 system rows × 12 fixed-order college columns + TOTAL column + totals row + subtitle); the 'N encoded without category' count sits as a card-level footnote, visible even when the toggle is collapsed or the card shows its empty state, because uncategorized records are absent from the chart too. Amends FR-ANL-02/03. ⚠️ **Moot as of D-32** (FR-ANL-02/03 superseded) — row kept for the audit trail. | Two stacked cards showing identical numbers in different shapes was redundant screen space; folding the matrix into the existing table toggle keeps one card = one dataset while still satisfying FR-ANL-03's matrix format and the chart's low-contrast-series relief in a single table. |
| D-31 | **Each Cases-by-Medical-System bar (FR-ANL-08) splits into Male + Female segments.** The Male segment keeps the system's full-strength FR-ANL-02 series color; the Female segment is a **50%-white tint of the same color, derived in code** (never hand-picked, so the pair can't drift if a series color changes). The stacked bar's full length is still the per-system total, which is drawn as a figure at the bar's end (with two segments, neither shows the total alone); the card subtitle states the convention (stronger shade = Male, lighter = Female) in place of a legend, and tooltips give the exact split. Source gains a join to `student_profiles.sex`; counting rules unchanged (encoded only per FR-ANL-07, one count per record × category per D-23). Amends FR-ANL-08. ⚠️ **Moot as of D-32** (FR-ANL-08 superseded) — row kept for the audit trail. | The Director's reporting need is "case category … by sex" (§3 persona, G3); folding the sex dimension into the existing per-system bars answers it with zero extra cards, and color-by-category with a shade-by-sex keeps the chart readable against the college chart's established series colors. |
| D-32 | **Director analytics rescoped to system-collected data — drops the Medical Cases analytics and the case-category concept.** FR-ANL-02 (Medical Cases by College), FR-ANL-03 (Summary of Medical Cases matrix), FR-ANL-06 (Export/print) and FR-ANL-08 (Cases by Medical System) are **superseded**, and the case-category concept is removed system-wide. **Supersedes D-23:** table #11 `clearance_case_categories` is dropped and the schema returns to **10 tables**. **Amends the locked decision** "the Nurse encodes Fit/Unfit + case category" → the Nurse encodes **Fit/Unfit only**. **D-30 and D-31 become moot** (their rows stay for the audit trail — a documented descope is a defense strength, not a gap). **FR-ANL-06 is removed entirely** — analytics is on-screen only: no monthly "Summary of Medical Cases" print, no analytics CSV, no Flagged-Anomalies CSV. The replacement reads only data HealthPass itself collects — appointments booked in the web app and vitals captured at the kiosk: FR-ANL-09 (Clinic Visits by College + Visits by Purpose), FR-ANL-10 (Vital-Sign Flags with rates), FR-ANL-11 (Visits per Month trend), FR-ANL-12 (BMI Distribution), FR-ANL-13 (month + college filters); FR-ANL-04 (retitled "Students Screened by Sex", now college-filtered) and FR-ANL-05 (Category column dropped) are amended. | HealthPass only ever sees clearance/kiosk encounters, never walk-in consultations, so a "Summary of Medical Cases" cannot represent the clinic's caseload — a direct defense risk ("does this include walk-ins?" — no). The replacement is the honest census of the system's own data. Encoding Fit/Unfit only sheds a data-entry step borrowed from a different clinic function (manual case logging) that HealthPass was never the system of record for. |
| D-33 | **Dental appointments link at the kiosk — amends D-3.** Under D-3 a dental-only student already runs the full kiosk flow, but the visit is stored as a walk-in (`appointment_id` NULL), so the dental appointment is never completed. New rule: **kiosk submit links today's dental appointment** (server-side resolution, same kiosk trust rules — client-supplied identity is never trusted). The flow is otherwise **identical** — vitals + questionnaire + nurse queue; the existing nurse-encode step already marks the linked appointment `completed`, which now works for dental too and gives dental visits a capture-time `clinic_visits.college_id` snapshot. **Edge rule:** if a student has BOTH a medical and a dental appointment today, the **medical appointment wins** the link and the dental one stays `scheduled` (rare; recorded as a stated limitation in FR-ANL-09 / the kiosk FRs). Dental remains scheduling-only in every other respect (no clearance form, no Fit/Unfit, no questionnaire branch). | Completing dental appointments and snapshotting their college is exactly what the new Clinic Visits by College analytics (FR-ANL-09) needs — without the link, dental visits were invisible to the system's own census. Reusing the existing submit-link + encode-complete path adds the dental dimension with no new screens. |
| D-34 | **The internet deployment is the real deployment; the defense demos it. Supersedes D-9.** HealthPass ships as **one hosted Laravel app over HTTPS** (VPS + domain + Let's Encrypt) serving the web app and the kiosk alike. At the defense the Raspberry Pi is a **terminal, not a server**: Chromium opens `https://<domain>/kiosk`. Internet deployment moves from "optional, post-stability" to **in scope and mandatory**. Consequences, all of them deployment-level (no feature behaviour changes): (a) **`TRUSTED_PROXIES`** must name the real reverse-proxy IP(s) — never `*`, which `App\Support\TrustedProxies` rejects at boot; without it every visitor's IP reads as the proxy's, collapsing all per-IP throttles (OTP, kiosk POSTs) into one shared bucket and generating `http://` links on an `https://` site. Applied in `AppServiceProvider::boot()`, **not** `bootstrap/app.php`, whose middleware closure runs before the config files load (`env()` there reads empty once `config:cache` has run). (b) **`HEALTHPASS_KIOSK_ALLOW_LOOPBACK=false`** — a same-host proxy makes every visitor look like `127.0.0.1`. (c) The Pi must be **enrolled as a kiosk device** (D-27) and its Chromium profile must persist — **`--incognito` is removed from `scripts/pi/kiosk-chromium.sh`**, since it wiped both the device-token cookie and the Web Serial grant on every launch (this supersedes D-27's "for the Pi's incognito launcher" note). (d) **Web Serial grants are per-origin**, so the ESP32 must be re-authorized once on the new HTTPS origin; any Chromium `SerialAllowUsbDevicesForUrls` policy must name that origin. (e) HTTPS is forced **at nginx**, never keyed on `APP_ENV` — the fallback Pi is `APP_ENV=production` over plain `http://localhost` by design. **D-9's Pi-local shape is retained as the documented, rehearsed offline fallback** for venue-internet failure (`docs/deployment-pi.md` §10); the fallback's separate database must be refreshed from a hosted dump before defense day. New runbook: `docs/deployment-hosted.md`. | A capstone that only runs on one Raspberry Pi in one room is a prototype; a system the panel can open on their own phone is a deployed product, and "is this actually usable by the university?" is the question a panel asks. The Pi-local shape was chosen when TLS looked like an obstacle — HTTPS satisfies the same secure-context rule that `localhost` did, so the constraint that forced D-9 no longer binds. Keeping D-9 alive as a tested fallback costs one rehearsal and removes the single-point-of-failure that hosting introduces (venue Wi-Fi). |
| D-35 | **Staff credentials: seeded accounts ship one-time passwords that must be changed at first login — schema change `users.must_change_password` (boolean, default false), implemented.** **Staff accounts stay seeded (FR-AUTH-05 unchanged) but ship with real emails and one-time passwords that must be changed at first login.** `StaffSeeder` keeps creating the 1 director + 1 nurse + 12 college admins and gains two config switches (`healthpass.seed_staff.one_time_passwords`, `healthpass.seed_staff.email_domain`, read via `config()` not `env()` so `config:cache` cannot silently revert them). Default OFF preserves the local shared `password` / `@healthpass.test` accounts exactly as documented in `docs/dev-notes.md`. ON — the hosted deploy — issues a **distinct 16-character random password per account**, printed once by the seeder and stored nowhere in plaintext, sets `must_change_password = true`, and builds addresses at the real institutional domain. `RequirePasswordChange` middleware (appended to the **web group**, so it cannot be forgotten on a route) then redirects every flagged user to the D-20 change-password screen, allowing only that flow and logout through, and answering JSON requests with 403 rather than an HTML redirect a background poll would mis-parse. The flag is cleared in exactly one place — the OTP-confirmed apply step — so it can never be cleared without a verified code. Students are unaffected (they self-register and set their own password). **Rejected alternative:** a public/College-Admin self-registration flow approved by the Director — it contradicts **FR-AUTH-05 (M)** ("no public staff registration path shall exist"), and would add screens, states, notification mail, and tests to provision **14 fixed accounts** that change roughly never. | Going live on the internet with 14 accounts sharing the password `password` and `@healthpass.test` addresses is the single largest exposure D-34 introduces — the same seeder that is correct for a laptop demo is a breach on a public domain. Forcing the change at first login keeps provisioning a one-command seed (no new screens, no approval workflow) while guaranteeing no shipped credential survives contact with production. An approval workflow would be a new feature solving a problem the fixed, tiny staff roster does not have. |
| D-36 | **Batch approval is confirm-only; rejection requires a written reason — flagged schema change (`batch_requests` gains `rejection_reason` TEXT NULL). SUPERSEDES the "the Director may adjust" clause of D-29** (D-29's other half — the admin proposes the date — stands, and D-29 is marked superseded in place rather than deleted). (a) **Approve loses its date picker.** The modal shows the College Admin's `requested_date` **read-only**; the batch is approved on that date, read from the row **locked inside the transaction**. `ApproveBatchRequest` accepts **no input at all** (`rules()` returns `[]`), so a `scheduled_date` posted by a client — the field the pre-D-36 modal used to send — is inert; a feature test asserts an injected date is ignored. (b) **Two kinds of batch can no longer be approved at all**, because neither leaves a date to confirm: **pre-D-29 batches (`requested_date` NULL)** — reject with "predates the requested-date field — please resubmit" — and batches whose **requested date passed while they sat pending**, since confirm-only cannot move the date and approving would schedule the cohort in the past. In both cases Approve is disabled on the row with a one-line notice **and** the endpoint refuses the POST (the UI is never the gate); the staleness rule lives in one place, `BatchRequest::hasStaleRequestedDate()`, called by the view and by the endpoint on the locked row. A batch requested **for today** is still confirmable — the boundary is strictly "before today". (c) **Reject now requires a reason** — required, 10–500 characters, validated by a new `App\Http\Requests\Director\RejectBatchRequest` (the old `reject()` took a bare `Request`, which is exactly why it had no validation); the modal's live counter and disabled-until-valid submit are a client convenience only. The existing `lockForUpdate()` + already-decided no-op guard is unchanged, so a replayed reject can never overwrite a reason already on record. (d) **The College Admin reads the reason back** on Batch Tracking (FR-ADM-05): a Rejection Reason column appears beside Status **only when a rejected row exists**, with a View button opening a modal showing the reason, the reviewing Director and `reviewed_at`; the reason is escaped on output (`{{ }}` / `Js::from` → `x-text`, never `{!! !!}`) because it is one user's free text rendered to another. Column works in both the desktop table and the mobile stacked cards. **The FR-DIRA-06 capacity behaviour is unchanged by this decision — it remains a warning, not a block.** Amends FR-ADM-05, FR-DIRA-02/04. | A silent date move is an **audit-trail hole**: the College Admin submitted for a date tied to a real event (OJT start, graduation), and under D-29 the Director could change it to something else with the cohort finding out only when appointments appeared on the wrong day — nothing in the record said who changed it, or why. Reject-with-a-reason produces the opposite: the original request stays intact, the objection is written down against the batch, and the college resubmits for a date it has actually agreed to. It also removes a real trust-boundary risk — an approve endpoint that accepted a client date was one modal bug (or one crafted POST) away from scheduling a cohort on a date nobody chose, and the server now derives the date entirely from its own locked row. The cost is one extra round trip when a date genuinely doesn't work, which is the rare case; the benefit is that every date change is visible to both parties. Making the reason mandatory (10 chars, not just "required") is what stops the audit trail degrading into a column full of "no". |
| D-37 | **Appointments get a TIME: the clinic day becomes ten one-hour slots of 12, capacity 40/day → 120/day — flagged schema change (`appointments.scheduled_time` TIME NULL; `batch_requests.requested_time` TIME NULL + `requested_blocks` TINYINT NULL). Amends BR-01/BR-02/BR-04, adds BR-21/BR-22, amends FR-STU-03/04, FR-ADM-04, FR-DIRA-02, and SUPERSEDES FR-DIRA-06's warn-but-allow with a hard block.** (a) **The grid.** `healthpass.clinic_hours` (7 AM–5 PM) now yields ten bookable one-hour slots, 7–8 AM through 4–5 PM, **lunch (12–1 PM) included** — the clinic is staffed through it and excluding it would cost 12 seats a day for no operational reason. The list is derived at runtime by `App\Services\ClinicScheduleService`, never typed into a view, so changing the config changes the calendar, the batch form and the Director modal at once. Slot keys are the canonical string `'07:00:00'` everywhere — `<select>` value, DB column, WHERE clause — because MySQL TIME and SQLite TEXT round-trip that form identically and a stray `'07:00'` would pass the SQLite suite while miscounting on MySQL. (b) **The numbers.** A kiosk session takes at most 5 minutes → 12 students an hour (`hourly_capacity`), × 10 hours = **`daily_capacity` 120, up from 40**. Both stay **config values**, not controller constants. The daily cap is kept alongside the hourly one rather than deleted, because it is the only counter that can see pre-D-37 appointments. (c) **Medical and dental share ONE counter.** A dental appointment consumes an hour-seat exactly like a medical one: what is being capped is **clinic congestion** — how many students are in the clinic at once — not kiosk throughput alone, and a dental patient occupies the same room, queue and staff attention. (d) **Legacy rows are NOT backfilled.** Pre-D-37 appointments keep `scheduled_time` NULL, render as an em dash, and are invisible to the per-slot counters. Backfilling them to 07:00 would have invented twelve fake bookings in the first hour of every past clinic day and corrupted exactly the counts the column exists to make correct. (e) **Enforcement is doubled, deliberately.** Both the student booking and the batch submission check capacity in their Form Request **and again under `lockForUpdate()` inside the write transaction** — the Form Request's read is unlocked and races with a concurrent booking for the last seat in an hour, so the locked re-check is the authoritative gate and enforcement must never move into the Form Request alone. A feature test injects a competing booking at the exact moment the unlocked read returns, and asserts only one booking wins. (f) **Batches occupy a span.** `ceil(students ÷ 12)` contiguous hours from an admin-chosen start, shown back live as *"30 students → 7:00 AM – 10:00 AM (3 slots)"*; rejected if any hour in the span is full (**the error names the hour**), if the span crosses 5 PM, or if the roster exceeds 120. The span LENGTH is always recomputed server-side, so a posted `requested_blocks` is inert. The stored representation is **start + block count, never an end time and never both** — see the data dictionary for the reasoning. (g) **FR-DIRA-06 flips from warning to HARD BLOCK.** A full hour anywhere in a batch's span refuses approval, in the UI *and* at the endpoint. **Stated plainly: combined with D-36's confirm-only approval, a batch whose slots filled up while it sat pending has exactly one outcome — rejection and resubmission. That is the intended workflow, not a bug.** A pre-D-37 batch (no `requested_time`) is a third un-approvable case alongside D-36's two, with the same reject-and-resubmit remedy. (h) **The fan-out** writes `scheduled_time` onto every generated appointment, 12 per hour in **pivot-row id order** — deterministic, with the last block taking the remainder. (i) **An hour that has already ended cannot be booked on today (BR-23).** A slot dies when it **ends**, not when it starts — at 12:00 the 11 AM–12 PM hour is gone, 12–1 PM is not — which keeps it consistent with BR-20 rather than contradicting it. It binds the **student** picker and the College Admin's batch **start hour** alike; only the span's start needs checking, since a span runs forwards. Decided on the server clock and shipped to both pages as data, never computed in the browser. This also means a date whose remaining hours have all elapsed greys out in the calendar, which makes BR-20's after-closing rule a natural consequence rather than a separate special case. **Not** re-checked under lock, unlike capacity: elapsed-ness is monotonic and uncontended, so there is no race to lose. It binds **Director approval** as well: D-36 still allows approving a batch requested for today, so its hours can end while it sits pending, and fanning out then would create appointments for a time already gone — any elapsed hour in the span (even a partially-elapsed one) refuses approval on the same reject-and-resubmit terms as a full one. | A single daily number could not express the real constraint. Forty students spread over a day and forty arriving at 7 AM are the same number and completely different clinics: the second is a two-hour queue at one kiosk and one nurse, which is precisely the queueing the system exists to remove. Modelling the hour is what makes "book an appointment" mean a time a student can actually turn up at, and it is what lets the cap rise from 40 to 120 — the old 40 was low because it was implicitly rationing a whole day's congestion through one number. Keeping the warn-but-allow at approval would have re-opened the same hole from the batch side: a Director could approve 30 students into an hour that already held 12, and no per-hour cap anywhere else would have mattered. The cost is one extra round trip when a cohort's hours fill up — the rare case — and the benefit is that the schedule the app shows is a schedule the clinic can actually serve. |
| D-38 | **Site-wide light/dark theme for the web app — no schema change; kiosk and print views excluded.** Every web-app page (all four roles + the auth/guest pages) gains a light/dark toggle in the top-right of the sticky header (`components/layout/sidebar.blade.php`); the auth/guest shells, which have no header, carry the same toggle fixed top-right so a visitor whose OS is dark can switch **before** logging in. Implementation is **token indirection, not a `dark:` sweep**: the five `--hp-*` colour tokens in `resources/css/app.css` change from hex to bare `R G B` channel triplets, Tailwind maps `hp-*` to `rgb(var(--hp-…) / <alpha-value>)`, and a single `.dark, [data-theme="dark"]` block re-points them — so ~300 existing `text-hp-slate/NN` and `border-hp-slate/NN` utilities re-theme untouched. Consequences: (a) **a colour token used in plain CSS must now be wrapped — `rgb(var(--hp-bg))`, never `var(--hp-bg)`** (a bare triplet is not a valid `<color>`; this bit the kiosk panel's inline style and is now commented at both call sites); (b) `bg-white` was swept to `bg-hp-white` in non-kiosk views (`text-white` deliberately was **not** — it is the label on the orange button and must stay literal); (c) muted-text alphas, tuned for a light background, get dark-only bumps (`/20 /25 → /50`, `/30 /35 /40 → /55`, `/45 /50 → /60`, `/55 → /65`) so every muted step clears 4.5:1; (d) modal scrims switch from `bg-hp-slate/NN` to `dark:bg-black/60`, since slate inverts to near-white and would veil the page in light; (e) Chart.js bakes colours in at construction, so the Director analytics bundle reads the tokens at runtime and re-colours on a `hp:themechange` event. **Persistence is `localStorage` only** (`hp-theme`), defaulting to `prefers-color-scheme` — **no `users` column, no POST, no migration**. The theme class is set by an inline `<head>` script (`partials/theme-init.blade.php`) before first paint, the same FOUC-prevention pattern as `window.__hpSidebarCollapsed`; the inline splash overlay is themed the same way or it would flash cream on every slow load. Transitions run on the `--hp-dur-base` / `--hp-ease-out` tokens via a temporary `.hp-theming` class applied only while a toggle is in flight, deliberately **without `!important`** so the existing `prefers-reduced-motion` kill switch still wins. **Excluded: `resources/views/kiosk/**`** (never includes theme-init, has no toggle, keeps the light tokens — D-26) **and all print output** (`nurse/print.blade.php` is a standalone document that loads no bundle and renders into an iframe, which cannot inherit `<html>` classes; a `@media print` block additionally re-points the tokens to light for any app page sent to a printer while dark). Palette adjustments and the measured contrast figures are in §8.1. Adds **FR-UI-04 / NFR-10**. | Students and staff use this on their own phones and laptops at night once the app is on the public internet (D-34), and a cream `#F6F2ED` full-screen app is genuinely uncomfortable in the dark. Driving it through the existing token layer means the feature is ~2 CSS blocks plus a toggle rather than `dark:` variants smeared across 60 view files, which is what keeps it maintainable by two Laravel beginners. The kiosk is excluded because it is a fixed appliance whose contrast was tuned for a lit clinic at standing distance and has no user to own a preference; print is excluded because the output must match the official form DHVSU-QSP-OSS-004-FO002-R03 on paper. |
| D-39 | **Appointment emails are QUEUED and dispatched after commit; batch appointments become non-student-cancellable.** Adds **FR-STU-12** and amends **FR-STU-06**. (1) One Mailable (`App\Mail\AppointmentScheduledMail`) serves both creation paths, sent through one queued job **per student** (`App\Jobs\SendAppointmentScheduledMail`, `tries=3`, `backoff=[60,300]`, `deleteWhenMissingModels`), dispatched **after `DB::transaction()` returns** in both `BookAppointmentController::store()` and `BatchApprovalController::approve()`. Queued rather than synchronous so a 60-student approval costs the Director 60 INSERTs rather than 60 SMTP round-trips; **after commit** rather than inside because a send that throws inside the transaction would roll back real appointments, and a worker could otherwise pick up an appointment id its own connection cannot yet see. **One job per student, not one job looping the roster**, so a single undeliverable address fails only its own message. The job re-checks `status === 'scheduled'` at send time, so a student who cancels while the job waits is not then told their appointment is confirmed. No new package: with the default single `queue:work` process jobs are consumed serially, which is itself the send-rate ceiling — if a provider imposes a stricter cap, keep one worker rather than adding a throttling library. (2) `healthpass.clinic_location` added as a config value (the clinic can move; the kiosk already did) — **no schema change anywhere in this decision.** (3) **Batch appointments are no longer student-cancellable.** The pre-existing `cancel()` guarded only ownership, `scheduled` status and a future date, so a batch student *could* silently drop out of a cohort their College Admin had booked, leaving the college's roster wrong with nobody informed. `Appointment::isSelfCancellable()` is now the one rule, read by the endpoint and by both views that draw a cancel button. | Students booked into a batch had **no notification of any kind** — the college admin booked them and the first they would learn of it was not turning up. Email is the only channel the project has (no SMS budget, no push), and the OTP mailer (D-8) already proves the path works. The after-commit ordering is the whole point of the decision: the alternative, dispatching inside the transaction, converts a mail-server outage into lost appointments, which is strictly worse than a late email. The FR-STU-06 amendment is a **consequence, not a side quest** — the email has to tell batch students how to cancel, and "contact your college admin" was a false statement until the endpoint actually enforced it; shipping the mail without the guard would have put an untruth in front of real students. **Known gap at the time of this decision: the College Admin had no cancel/withdraw endpoint** — closed immediately afterwards by **D-40 / FR-ADM-07**. |
| D-40 | **College Admins can withdraw one student's appointment from an approved batch; batches get a roster page.** Adds **FR-ADM-07**. **No schema change.** The other half of D-39: that decision stopped batch students cancelling their own appointment and pointed them at their College Admin, but the admin had no way to act — Batch Tracking showed only a student *count*, and there was no route, controller method or UI anywhere for cancelling an appointment. New `GET /admin/batches/{batch}` roster page (students, appointment reference, hour slot, status) and `DELETE /admin/batches/{batch}/appointments/{appointment}`. **Withdrawal is a status flip to `cancelled`, and that alone frees the seat** — every capacity count in the app (daily cap, D-37 per-hour cap, the calendar's full-day roll-up) already filters on `status != 'cancelled'`, so no counter is maintained anywhere and none can drift. The row is kept rather than deleted and the pivot's `appointment_id` retained, so a withdrawn student stays visible in the batch's history instead of vanishing from it. Guards: `scheduled` only (a `checked_in`/`completed` appointment may already be referenced by a `clinic_visits` row, so cancelling it would contradict a real encounter), and not a past date; **today remains withdrawable**, deliberately unlike the student rule, because "phoned in sick this morning" is the commonest reason a seat needs freeing and blocking it would strand a seat everyone knows is unused. `Appointment::isAdminCancellable()` is the single rule shared by the view and the endpoint, re-read **under `lockForUpdate()`** in the transaction. Scope follows FR-ADM-06 exactly — the batch comes from `managedCollege()->batchRequests()` and the appointment must belong to it, so neither a foreign batch id nor a foreign appointment id is reachable. | D-39 created an obligation the app could not honour: the appointment email tells batch students to contact their college admin, and until this decision that admin could only phone the clinic. Scoping withdrawal to **one student at a time** rather than a whole-batch cancel matches the case the email actually generates (a single student drops out); cancelling an entire approved cohort remains a Director-side concern and is deliberately not built here. Keeping the cancelled row and its pivot link is what lets a college answer "was this student ever booked?" months later — a hard delete would make a withdrawn student indistinguishable from one never submitted. The `scheduled`-only guard is the important safety rule: it is the boundary past which an appointment stops being a reservation and starts being evidence of a clinic visit. **Note: this decision as first written left the withdrawn student unnotified — closed immediately afterwards by D-41 / FR-STU-13.** |
| D-41 | **A withdrawn student is emailed; the two appointment notices share one queued-job base class.** Adds **FR-STU-13**. **No schema change.** Completes the D-39/D-40 pair: D-39 emailed the student when their college booked them and stopped them cancelling it themselves, D-40 gave the College Admin the withdraw action — but the student was told nothing when it fired, so a cancelled student would still have turned up. `App\Mail\AppointmentWithdrawnMail` + `App\Jobs\SendAppointmentWithdrawnMail`, dispatched from `BatchRequestController::cancelAppointment()` **after the transaction commits**, on exactly the terms D-39 set. Only a withdrawal that actually happened emails anyone — a refusal (checked in, completed, past date, duplicate submit) returns before the dispatch, which is asserted by test. The notice tells the student they need not attend, names what was cancelled, and **points them at self-booking**: a withdrawn student is an ordinary student again, so they need not wait for their college to arrange another batch. **A self-booked cancellation deliberately sends nothing** — the student did it themselves and an email confirming their own click is noise. Structurally, the two notices now share an abstract `App\Jobs\AppointmentMailJob` carrying the retry policy (3 tries, 60s/300s backoff), `deleteWhenMissingModels`, recipient resolution from the User record, and PII-free failure logging; each subclass supplies only its Mailable, its still-relevant check and a log label. **Implementation note: the shared `$appointment` property must NOT be `readonly`** — PHP only permits a readonly property to be initialized from its declaring class, and the queue rehydrates a job onto an instance of the *subclass*, which throws "Cannot initialize readonly property … from scope" the moment the job round-trips. | Without this, D-40 shipped a silent failure of exactly the kind D-39 existed to fix: the student had been told in writing that an appointment existed, could not cancel it themselves, and would then have received nothing when it disappeared. Pointing them at self-booking rather than only back at their college is what stops a withdrawal turning into a lost clearance — the student can act immediately. The base class was extracted rather than the second job copy-pasted because the retry policy and the "no PII in logs" rule are exactly the sort of thing that drifts between two near-identical files and then only fails in production. |
| D-42 | **Academic programs become a validated, college-dependent catalog held in config, not a table.** `student_profiles.course` keeps its name and stays a `string`; majors are FLAT entries in one dropdown ("Bachelor of Secondary Education major in English" is its own option, not a second cascade level); year level becomes college-dependent from the same file. **NO schema change.** The catalog is `config/programs.php`, keyed by college **code** (so it survives a reseed that renumbers college ids), each entry holding a `programs` list and a `year_levels` map of storage-key ⇒ display-label. `App\Support\Programs` is the only bridge from a college **id** — what the forms post and what `student_profiles` stores — to that code, and it never throws: an unknown college returns an empty list, so a hand-edited form fails validation instead of 500ing. `StoreRegistrationInfoRequest` and `UpdateStudentProfileRequest` both validate `course` and `year_level` with `Rule::in()` built from the **submitted** college; the Alpine cascade on the two forms is UX only. **Four editorial calls** on the adviser's raw list, all flagged for confirmation: (1) COE listed "Bachelor of Technical-Vocational Teacher Education" twice — once with two majors, once bare — recorded as three entries (two majored tracks plus one bare general track), no literal duplicate; (2) "Methods of Teaching" was indented under the BSEd majors, so it is recorded as "Bachelor of Secondary Education major in Methods of Teaching"; (3) Graduate Studies entries carrying "(Academic and Professional Track)" keep the base program name only — a track is not a separate program — the one exception being MPA major in Regulatory Management Systems, kept because it names a **major**; (4) the "(New!)" marker was dropped from Master in Information Technology and the doubled "in in" fixed in the CAS Biology entry. Year levels are per **college**, not per program: Associate in Computer Technology is a 2-year program inside a 1–4 college and therefore offers years 1–4 here — per-program year levels are deliberately not modelled. Senior High School is absent from the catalog because D-43 removes it from the college list. | The adviser requires monthly clinic reports broken down per academic program per college, and free text cannot be grouped: "BSIT", "BS Info Tech" and "Bachelor of Science in Information Technology" are three programs to a `GROUP BY` and one to a human. Config rather than a table because there is **no admin CRUD requirement** for programs and a table would push the canonical schema past 10 — D-32 having just brought it back to 10 — while a fixed catalog needs no migration, no foreign key and no backfill, and a future admin-editable table stays additive. The column keeps the name `course` because the official DHVSU form (D-25) prints "Course, Year & Section": `course` is the correct domain term even though the UI labels the field "Program". Majors are flat because a second cascade level would double the UI's states to buy nothing — nothing in the system reports on a major independently of its program. Year level moved into the same file because the hardcoded 1–5 list was already wrong for two units: Graduate Studies has no 3rd year and Laboratory High School's students are in Grades 7–10, not years. |
| D-43 | **Senior High School is removed from the college list (12 → 11); clinic visits snapshot the student's program at capture.** **FLAGGED SCHEMA CHANGE: `clinic_visits.course` VARCHAR(120) NULL**, nullable and **never backfilled**, mirroring the `college_id` snapshot of D-17. (1) **SHS removal.** SHS is not on the PSU main-campus (bicolor) program offerings, so it is not a unit this system serves and it is absent from the D-42 catalog. It was wired into more than one place: `CollegeSeeder` (the row), `StaffSeeder` (an SHS administrator account), `StudentSeeder` (2 students), `DemoClinicVisitSeeder` (a visit-volume weight), and the college tables in this document and `HealthPass_Context.md`. All are gone; the seeded college-admin count drops 12 → 11 and the demo cohort 30 → 28. A `CollegeSeederTest` now asserts the count, the absence of the code, and — the invariant that made the removal necessary — that **every seeded college has a `config/programs.php` entry**, since a college without one leaves its students an empty Program dropdown they cannot register through. (2) **Program snapshot.** `SubmitKioskVisit` freezes the student's `course` alongside their `college_id`, both read in ONE query from the **server-side bound student**, never the request body (kiosk endpoints trust no client-supplied identity). A missing college still fails loudly — the visit could not be attributed at all — but a **missing program stores NULL** and never blocks the person standing at the kiosk. (3) **A live bug fixed on the way.** The app stores `year_level` as a KEY ('1'…'5', '7'…'10') and validates against that set, but the seeder and factory wrote DISPLAY LABELS ('3rd Year', 'Grade 11'). Nothing re-validates a profile on read, so the mismatch stayed invisible until a demo account tried to save its own profile and got "Please select a valid year level". Both now write keys, `StudentProfileFactory` drops its hand-written course/year arrays in favour of the D-42 catalog, and a regression test saves an unchanged seeded profile for both shapes of the bug (an ordinary 1–4 college and junior high's grade numbers). | Per-program reporting must be **transfer-proof and shift-proof** for the same reason D-17 froze the college: `student_profiles.course` stays live, so without a snapshot one student shifting program would silently restate every monthly report they appear in — the report for a month already printed and handed to a college would no longer reproduce. The column is nullable and never backfilled because a visit captured before this decision has **no honest answer**: the student's current program is a guess, not a record, and writing a guess into a reporting column is worse than writing nothing. Those rows render "—", the pattern `appointments.scheduled_time` already established for pre-D-37 rows. No new table: the value is a copied string, exactly like the college id it sits beside, and the 10-table canon is unchanged. The seeder's course list had already drifted from reality — it put a CAS student on Psychology, a CSSP program — which free text happily stored and D-42's validation now rejects; one catalog for the forms, the factory and the seeders is the only arrangement that cannot drift again. |
| D-44 | **The nurse gets a dashboard carrying the encode log; the history is clinic-wide with an Encoded-by column, and the dashboard replaces the Live Queue as the nurse's landing page.** **NO schema change.** One page, `/nurse/dashboard` (`FR-NRS-09`): four stat tiles above the filterable, paginated encode history — not two pages and not a dashboard-plus-history pair. `EnsureRole`'s home map now sends a nurse to `/nurse/dashboard`; **Live Queue stays in the nav, unchanged**, and Save & Close still returns there so the reverse-stack ghost row (§6.1b) is untouched. The history reads `clearance_records` — that table IS the encode log, one row per encoded visit — joined out to the visit's student, capture-time college and **D-43 program snapshot** ("—" when null), and it is **CLINIC-WIDE, never scoped to the signed-in nurse**, which is why every row names its encoder. Filters are GET (`month`, `result`, `q`) so a filtered view is shareable and survives paging (`withQueryString()`); the month list reuses `App\Support\VisitMonths`, and a month the picker does not offer degrades to "all" rather than erroring, so the `<select>` and the table can never disagree. Rows reuse what exists — **no new detail page**: View opens the encode screen, which already renders read-only for an encoded visit, and Reprint posts to the existing reprint route (in a new tab, since the hidden print iframe and its script belong to the encode page and forking them would fork the print flow). Month bounds are Carbon, never `MONTH()`/`DATE_FORMAT` — the suite runs on SQLite. | This closes the adviser's **"every log the system produces should be visible on the relevant role's dashboard"** point, and he named the nurse's encode log specifically. The nurse was the **only role with no history surface**: students have My Records, College Admins have Batch Tracking, the Director has Batch Approvals and Flagged Anomalies — while an encoded visit left the Live Queue (`scopeLiveQueue` filters `status = 'captured'`) and became reachable only by typing its URL, so after a clinic day the nurse could not see what they had encoded. Clinic-wide rather than per-nurse because the clinic runs shifts: two nurses must read **one continuous log**, and "who encoded this" is answered by a column, not by hiding rows. It becomes the landing page because a log you have to navigate to is a log nobody reads, and the queue is one click away. |
| D-45 | **College Admins get analytics for their own college, broken down per program, built on a `ClinicAnalytics` service extracted from the Director controller so the two roles cannot drift.** Adds **FR-ADM-08**. **NO schema change** — the per-program split reads the `clinic_visits.course` snapshot D-43 already added. The adviser asked for the monthly report a college can request of its own students' clinic results, as analytics on the College Admin side rather than a new report format. The six existing card builders moved verbatim out of `Director\AnalyticsController` (which is now filter-resolution only) into `App\Services\ClinicAnalytics`, joined by one new builder, `visitsByProgram()`. The service takes its scope **at construction** (month, optional college, optional program), which is what makes it safe for the admin page: the College Admin controller constructs it from `managedCollege()` and there is no per-card college argument for a request parameter to reach. `?college=` is not validated-then-rejected on `/admin/analytics` — it is **never read**. The program filter is checked against the *managed* college's catalog (D-42), so another college's program is refused exactly like a made-up one. Two deliberate departures from a pure mirror of the Director page: the FR-ANL-11 trend stays **inside the college** (clinic-wide aggregates are still another college's numbers), and the month picker offers only months **this** college has data in — `VisitMonths::available()`/`resolve()` gained an optional `?College` for that, with the Director and Nurse call sites unchanged. Visits with a NULL program snapshot land in a **"Not specified"** row rather than being dropped. | Duplicating six aggregate queries into a second controller would guarantee drift: the first rule change would be applied to one page and not the other, and the two roles would quote different numbers off the same database — the worst possible failure at a defense. **The acceptance bar for the extraction was that `tests/Feature/Director/AnalyticsPageTest.php` (401 lines, 14 cases, exact counts) passed COMPLETELY UNEDITED**; any need to touch it would have meant behaviour had changed. Building the per-program split on the D-43 snapshot rather than the live profile is what makes a monthly report reproducible — re-running August's report in October must not move a student's visit to the program they shifted to in September. |
| D-46 | **The College Admin's monthly report is a PRINT VIEW (Blade + `window.print()`) rendering TABLES rather than charts; the Director's by-College card becomes a by-Program card when one college is selected.** Adds **FR-ADM-09**; amends **FR-ANL-09** and **FR-ANL-13**. **NO schema change**, no new package, no CSV — **FR-ANL-06's export remains struck by D-32**. The adviser's framing was that a college *requests* a monthly report of its students' clinic results — something a College Admin hands over, not only a screen they look at. D-45 built the screen; this builds the artifact. `GET /admin/analytics/print` (new `Admin\MonthlyReportController`, chosen over a second method on the analytics controller so the standalone-document response stays its own small file) renders `admin/monthly-report.blade.php`: a standalone Blade document with no app shell, the same shape as `nurse/print.blade.php`, loaded into a **hidden print frame** on the analytics page and printed there — the print dialog is the preview, so no tab is opened. The frame and its arming script live in the shared `partials/print-frame`, which the **Nurse Dashboard's Reprint now uses too** (it previously opened a new tab); the document self-prints only when it is the top window, so a middle-clicked link still works. It is **tables, not Chart.js canvases**, and it is **permanently light** (D-38). Its scope rule is FR-ADM-08's exactly: the college is `managedCollege()` and `?college=` is never read; only month + program carry over from the analytics page's query string, and a filtered report says so on the page. On the Director side, filtering to one college swaps Clinic Visits by College for Clinic Visits by Program via the existing `ClinicAnalytics::visitsByProgram()` — no new query logic. | A printed report is what actually leaves the clinic and reaches a dean's desk, and charts are exactly what fails there: Chart.js canvases print unreliably across browsers, and a bar without a number on it is useless on paper. Tables carry the same figures and survive photocopying. Building the report on the **same `ClinicAnalytics` under the same scope** is what guarantees the printout can never quote a different number than the screen it came from — the failure a defense panel would catch instantly. The Director's card swap is the same observation D-45 made one level up: a single-college bar chart compares nothing, while its programs are the breakdown the filter implies. Keeping `visitsByCollege()` populated alongside it meant `tests/Feature/Director/AnalyticsPageTest.php` needed **no edits to any existing assertion** — the same acceptance bar D-45 set. |
| D-47 | **The Clinic Director gains super-admin capabilities as a CAPABILITY LAYER on the existing role, NOT a fifth role.** Adds **FR-AUTH-10**; annotates **FR-AUTH-05**. **NO schema change.** `users.role` stays a four-value enum and **FR-AUTH-02 is unchanged** — a `super_admin` role would need an enum migration and would overturn a Mandatory requirement plus D-2, whereas a capability layer on the existing `director` role needs no migration at all. New `Director\StaffAccountController` + `director/staff.blade.php` behind the existing `['auth', 'role:director']` group: list, create, reissue password, activate/deactivate, reassign college. **Staff accounts are PROVISIONED by the Director**, reusing the **D-35** one-time-password flow verbatim — `Str::password(16, symbols: false)`, `Hash::make`, `must_change_password = true`, `email_verified_at = now()`, and the existing `RequirePasswordChange` middleware forcing the change at first login. The plaintext is **shown once** in a dismissible panel (flashed to a single request), never emailed and never persisted in readable form, exactly as `StaffSeeder::reportOneTimePasswords()` documents. **Anti-escalation** is server-side in three places: the `role` whitelist lives in `StoreStaffAccountRequest` (so `role=director` posted with devtools is a validation failure, not an account), `guardManageable()` refuses any target that is not a `college_admin`/`nurse`, and it refuses the signed-in Director's own row (a Director cannot deactivate or re-credential themselves here). A `college_admin` can be neither created nor left without a `managed_college_id`, since `college.scope` 403s such an account on every `/admin` route. **Deactivation is the only removal** — `clearance_records.encoded_by` and `batch_requests.reviewed_by` are `restrictOnDelete`, so nothing is ever deleted and history is untouched. **Deactivation now also ends a session that is already open, which amends FR-AUTH-07:** making removal a button exposed a gap that was harmless while it was a manual database edit — `users.status` was read only by `LoginRequest`, so a revoked account kept working for the remainder of `SESSION_LIFETIME` (two hours by default). New `App\Http\Middleware\EnsureAccountIsActive`, appended to the **web group ahead of `RequirePasswordChange`** (an inactive account is turned away, never invited to set a new password), logs the user out, invalidates the session, issues a fresh CSRF token and returns them to the login page carrying the same wording `LoginRequest` uses; a JSON request gets **403** rather than an HTML redirect, the rule `RequirePasswordChange` already follows. **`StaffSeeder` is unchanged**; this is an in-app path alongside it, and the seeder remains the bootstrap for the first Director account. Each of the five routes carries its **own throttle prefix** (an authenticated inline throttle keys on the user id with no path, so a shared bucket would have coupled them to each other and to the batch endpoints). **Rejected alternative: self-registration with a Director approval queue** — it would contradict FR-AUTH-05, add a public unauthenticated endpoint where none exists today, and verify identity only by self-assertion. **Explicitly out of scope:** activity log / audit trail, college or program CRUD (D-42 keeps programs in config), any UI over capacity or the locked 140/90 threshold (D-37), impersonation, and student creation or deletion. **Separation-of-duties note:** the Director both approves clinical batches (FR-DIRA) and controls access. Accepted, because the Director is the clinic's data controller and provisioning is limited to clinic-facing staff — never to another Director, which is what stops the role self-propagating. | Our adviser asked whether the system has a super admin and assumed it was the Clinic Director; it did not have one, and the gap was **operational, not cosmetic**. Staff accounts could only be created by running `StaffSeeder`, so on the hosted deployment that is now primary (D-34) adding one College Admin required a developer with server *and* database access, and there was no in-app way to remove someone who had left or to reissue a forgotten password — the two things that actually happen to a staff roster. A capability layer was chosen over a fifth role because the whole requirement is satisfied by *who may call five endpoints*, and roles are the one part of this schema that other decisions (D-2, FR-AUTH-02/03) are pinned to. The provisioning-not-registration shape follows from FR-AUTH-05 already being Mandatory: in-app provisioning **satisfies** it, self-registration would have **contradicted** it. Reusing D-35's credential path rather than inventing a second one is what keeps "no shipped credential survives contact with production" true for accounts created after go-live, not just for the seeded fourteen. |
| D-47a | **Two adjustments to D-47 after review, both narrowing it.** **No schema change.** (1) **The Director's "reset password" action is REMOVED** — the endpoint, the button and its tests are gone, and `StaffAccountController` now generates a password in exactly one place, `store()`. A Director who can set an existing account's password can sign in as that person and read their medical records, which is precisely the impersonation D-47's own scope list rules out; keeping the button would have left a supported path to it. A staff member who forgets their password now uses the FR-AUTH-09 forgot-password OTP like everyone else. (2) **College reassignment is confirm-on-change, not select-then-Save** — the Save button beside the dropdown read as an unclear second step; choosing a college now raises a confirmation and submits on accept, and restores the previous selection on cancel (Cancel, Esc and a backdrop click all revert it), so the control never displays a college the admin is not actually assigned to. The dialog is the **app's own modal — the same teleported Alpine panel `x-logout-confirm` uses**, extracted as `components/college-reassign-confirm.blade.php`; a native `window.confirm()` was replaced because it is unstyled, undismissable by backdrop or Esc, and looks nothing like the rest of the app. The target college's name is read at runtime from the chosen option's `data-name` and never interpolated into a JavaScript string, because an apostrophe in a name would break the literal and silently skip the guard — the trap already documented on the kiosk-devices Revoke button. Also fixes `x-hp.select`, whose equal left/right padding let a long option label run underneath the browser's dropdown arrow (`pl-3 pr-9`). | The reset button was a privacy hole wearing a convenience hat: it is the one capability on that screen that would have let the Director read a colleague's records rather than merely administer their account, and no clinic workflow needs it — the OTP recovery path already existed for all four roles. Removing it also collapses the credential surface to a single moment in an account's life, which is far easier to reason about and to defend. The confirm-on-change swap is the same reasoning one level down: with no Save button there is no state in which the screen shows one college while the database holds another. |
| D-48 | **The Staff Accounts screen gains a "Last active" column. FLAGGED SCHEMA CHANGE: `users.last_active_at` TIMESTAMP NULL**, nullable, no default, and **never backfilled**. Stamped by `App\Http\Middleware\RecordLastActive`, appended to the web group **after** `EnsureAccountIsActive` and `RequirePasswordChange` — a request those two turn away is not the account being used, so being bounced at the door must not register as activity. The write is **throttled to at most once per account per minute** (`THRESHOLD_SECONDS = 60`): stamping on every request would put a database write on every page load, and the Live Queue alone polls every four seconds, whereas the question the column answers is measured in days. It is written with the **query builder, not Eloquent** — an Eloquent `update()` silently adds `updated_at` to the SET clause, which would turn the user record's "last edited" stamp into a second "last seen" stamp (caught by test, not by review). Rows never seen since the column existed render **"Never"**, the same rule `clinic_visits.course` (D-43) and `appointments.scheduled_time` (D-37) follow. | `users.status` answers "is this account allowed in", which the Director had been reading as "is this account in use" — a different question. A College Admin who left in June still shows Active until somebody thinks to deactivate them, and until now nothing in the system could tell the Director that the account had been dormant for two months. This is the signal that makes the Deactivate button actionable rather than guesswork. Not backfilled for the usual reason: an account unseen before the column existed has no honest timestamp, and inventing one would make a dormant account look freshly used. **Known limitation, accepted:** the column tracks requests, so a browser left open on the auto-polling Live Queue keeps a nurse looking active; at the day-level granularity the column exists for, this does not change any answer. |
| D-49 | **A College Activity Log, DERIVED from `batch_requests` rather than written to a new audit table.** Adds **FR-ADM-10**. **NO schema change, no new table - the canon stays at 10.** New `GET /admin/activity` (`Admin\ActivityLogController` + `admin/activity.blade.php`), plus an **Activity Log** entry in the College Admin sidebar. Each batch row is flattened into the events it already records: a **submission** (`requested_by`, `created_at`, student count, reason) and, once decided, an **approval or rejection** (`reviewed_by`, `reviewed_at`, and the D-36 `rejection_reason`), merged into one timeline newest-first and paginated 15 to a page. **The flattening happens in PHP, not as a SQL UNION** - a UNION of submission and decision rows needs raw SQL, and the suite runs on SQLite while dev and prod are MySQL; a college's batch history is tens of rows, so there is nothing to gain by risking that drift. Sorting is tie-broken so a decision outranks the submission it decided when both carry the same timestamp. Scope is the standard `/admin` rule: `managedCollege()`, with `?college=` never read (asserted by test). **Explicitly out of scope, each for a reason:** an `activity_logs` **table** (rejected - see the rationale column); **appointment withdrawals** (D-40), which cannot name their actor because `appointments` has no `cancelled_by`, and adding one was declined for now; **student self-bookings**, by far the highest-volume event, which would bury the admin actions the page exists to surface; **page views and other reads**, because a coordination log is not surveillance and read-logging a health system creates its own privacy problem; and **anything clinical**, since a College Admin must never see vitals or Fit/Unfit. | The trigger was a question rather than a bug: with FR-AUTH-10 making a second College Admin a two-minute job, "can two admins now step on each other" became a real operational question, and the answer was that neither could see what the other had done. The Director's decisions were worse - an approval or rejection changed a college's schedule and appeared nowhere the college could read as a history. **The derived shape is the whole point of this decision.** An append-only audit table is the textbook answer, and it was rejected here for three concrete reasons: it would be the **11th table** in a canon D-32 had just brought back to 10; it introduces a **write path on every logged action**, each of which is a place a future edit can forget to log, so the audit silently becomes incomplete precisely when it matters; and it can **disagree with the data it describes**, which is the failure a panel would find instantly - a log saying a batch was approved beside a batch that is still pending. Deriving inverts all three: no table, no write path, and the log is the same rows read a different way, so disagreement is not merely unlikely but impossible. The cost is honest and bounded - the log can only ever describe what leaves a row, which is why appointment withdrawals are absent rather than approximated from `updated_at`. If a full audit trail is ever genuinely required (logins, profile edits, reads), that is a separate decision with its own table, and this page does not block it. |

---

## 15. Risks & Mitigations

| ID | Risk | Likelihood / Impact | Mitigation |
|---|---|---|---|
| R-1 | **BP monitor lacks usable serial output / Web Serial misbehaves in unattended kiosk mode** — the #1 project risk | Med / High | Week-1 spike gates all purchases (FR-HW-04); manual entry path (D-7) guarantees a demo regardless; document Chromium permission-persistence quirks in W6 |
| R-2 | Aggressive scope: 2 Laravel beginners + hardware track in 12 weeks | High / High | W1 vertical slice to learn the lifecycle; weekly working checkpoints; hard W11 freeze; designated slip candidate = batch workflow |
| R-3 | Sensor reliability (noise, drift, repeatability) | Med / Med | W3/W6 reliability testing; plausibility validation (FR-KSK-08); retry affordance per step; manual override |
| R-4 | Web Serial is Chromium-only | Certain / Low | Kiosk officially supports Chromium only; never promised elsewhere (NFR-3) |
| R-5 | Print fidelity to the official form drifts | Med / Med | Dedicated print CSS; SM-3 sign-off with clinic staff in W7, re-verified at freeze |
| R-6 | **Capstone paper still describes the AI system** — defense mismatch | Certain if unaddressed / High | Documentation team owns title/scope revision as a parallel workstream with its own deadline; adviser review at W5 check-in |
| R-7 | Two-developer bus factor / merge chaos | Med / Med | Feature branches + PRs from day one; pairing on kiosk integration; no artifact passing outside Git |
| R-8 | Demo-day hardware failure | Med / High | Spares kit (FR-HW-07); manual-entry drill rehearsed; W12 backup plan |
| R-9 | OTP email deliverability in production | Low / Med | Dev uses log/Mailtrap; D-8 fallback to Breeze link verification |

---

## 16. Open Items (tracked, not blocking the build)

1. **W1 spike verdict** (due before any hardware purchase): Web Serial reading on the Pi + BP monitor serial confirmation — owner: Baldo.
2. **Paper title/scope revision** to the no-AI system — owner: documentation team; adviser-reviewed by W5.
3. **Capacity initial value** for `config/healthpass.php` — set with clinic staff input (recommendation: start at the clinic's realistic daily throughput; adjust in config only).
4. **Internet deployment target** (shared host vs VPS vs PaaS) — **now blocking, not optional** (D-34: the defense runs against the hosted site). Still open: the concrete host, the domain name, and who pays/administers it. Requirements are fixed — PHP 8.2+, MySQL/MariaDB, root or sudo for nginx + certbot (shared hosting without shell access will not do), and a stable public domain for the per-origin Web Serial grant. Owner: Nat; needed well before the fallback rehearsal, not on defense week.
5. ~~**Export format polish** (FR-ANL-06 CSV columns) — finalize with the Director's reporting needs in W9–W10.~~ **Dropped:** FR-ANL-06 (analytics export/print) was removed entirely by D-32 — analytics is on-screen only.

---

## 17. Traceability Snapshot (module → primary tables → primary screens)

| Module | Tables written | Screens |
|---|---|---|
| AUTH | users | Login |
| REG | users, student_profiles | Register wizard |
| STU | appointments | Student Dashboard, Book, Records, My ID |
| ADM | batch_requests, batch_request_students | Admin Dashboard, New Batch, Tracking |
| DIR-A | batch_requests, batch_request_students, appointments | Batch Approvals |
| KSK | clinic_visits, vital_signs, screening_responses | Kiosk flow (10 screens) |
| NRS | clearance_records, clinic_visits, appointments, kiosk_devices | Live Queue, Encode Result, Kiosk Devices |
| PRT | clearance_records (printed_at) | Print view (iframe) |
| ANL | (reads) clearance_records, vital_signs, student_profiles, colleges | Director Dashboard, Analytics, Flagged Anomalies |
| HW | (feeds) vital_signs | Kiosk vitals steps |

---

## 18. Glossary

| Term | Meaning |
|---|---|
| Batch request | A College Admin's request for clearances covering a list of students, requiring Director approval |
| Captured / Encoded | Clinic-visit states: kiosk-submitted vs nurse-finalized |
| Clearance record | The nurse-encoded outcome (Fit/Unfit + metadata) attached 1:0..1 to a visit |
| Flag | A rule-based boolean signal on a vital (fever / high BP / abnormal BMI) — never a diagnosis |
| Keyboard-wedge | A scanner that presents as a USB keyboard, "typing" the scanned code |
| Kiosk mode | Chromium's full-screen, chrome-less `--kiosk` launch mode |
| MCU | Microcontroller (Arduino/ESP32) aggregating the sensors |
| RA 10173 | The Philippine Data Privacy Act of 2012 |
| Walk-in | A kiosk visit with no same-day appointment (`appointment_id` NULL) |
| Web Serial API | Browser API for reading USB-serial devices; secure-context only; Chromium-only |

---

*End of HealthPass PRD v1.0 — June 11, 2026. The page-by-page UI spec and full column-level schema in `HealthPass_Context.md` are incorporated by reference; where documents conflict, this PRD and the finalized ERD govern.*
